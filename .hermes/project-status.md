# Project Status

- Date: 2026-08-21 America/New_York.
- Project/worktree: `web_listening` at `C:\Project\web_listening\.worktrees\issue-52`.
- Branch/base: `codex/issue-52-immutable-artifacts` from exact clean baseline `ffad51579ac9e7774db554a0e035ab89eee3472a`.
- Boundary: delegated implementation worker for GitHub Issue #52 under parent Epic #46 only. Sibling repositories, sibling worktrees, other Issues, crawler/search orchestration, acquisition-manifest production, live network, subagents, commits, pushes, PR/Issue mutation, merge, branch/worktree cleanup, and manager state remain off-limits.
- Authority: frozen Issue #48 `acquisition-manifest.v1` blob and artifact identities. SHA-256 is over canonical response entity bytes; the frozen `artifact_id` binds `manifest_version`, `source_run_id`, `normalized_source_identity`, and retrieval SHA-256.
- Delivered implementation: additive `artifact_blobs`, `artifact_versions`, `artifact_observations`, `artifact_lineage`, and durable `artifact_blob_retirements` tables plus `immutable-artifact-store.v1`. `ArtifactStore` provides one strict HTML/PDF/allowed-attachment path, exact #48 observation IDs, version IDs over the same frozen run/source/content tuple, deterministic lineage IDs, cross-URL byte deduplication without provenance collapse, same-source immutable version history, canonical portable artifact URIs/relative paths, deterministic gzip replay, no-overwrite publication, idempotent exact replay, and fail-closed conflicting replay. Blob semantics contain only byte/storage facts; MIME remains version/observation provenance. Derived observations bind the exact selected `source_artifact_id` as source, parent, discovery reference, and one `derived_from` member. Loaded observations revalidate exact manifest/run/time/2xx/encoding fields, MIME plus content magic, reference existence, relation cardinality/ordinals, complete lineage role semantics, global DAG acyclicity, frozen provenance, and exact canonical `_blobs/<prefix>/<digest>[.gz]` paths.
- Validation and safety: response MIME header, filename/URL extension, content magic, access-decision identity, lowercase adapter identity, numeric adapter SemVer, closed contiguous redirect evidence, and closed discovery-kind semantics are checked before mutation. Defaults are valid #48 seed evidence rather than empty placeholders. Gzip uses zero `mtime` and normalized OS byte 255 for Python 3.12/Windows/POSIX portability. CAS publication and reads pin the root and target parent end-to-end; native directory-relative no-follow operations own exclusive temporary creation, exact-byte verification, hard-link publication/readback, and identity cleanup, while the Windows fallback holds directory handles and rechecks lexical ancestors. Link and secure/fallback rename calls are reconciled when they take effect before raising, preserving the primary exception without an orphan or lost original. Every public method on one `Storage` waits on a foreign transaction owner and remains reentrant for its owner; only the narrow #51 cross-thread rollback/cleanup handoff bypasses that wait. Store/get/replay/resolve/read/retention hold complete serialized lifecycles. Cancellation and injected failures roll back all rows and invocation-owned files. Retention commits a retirement marker with blob-row deletion while holding the SQLite write lock; triggers prevent independent processes from committing new document/blob/tracked/acquisition references to retired bytes. Immutable resurrection clears retirement atomically with the new blob/references. Post-commit compensation restores and verifies the exact canonical leaf before row insertion/retirement removal; collision keeps delete and retirement durable, leaves the original quarantined, preserves the primary error, and never inserts a row pointing at replacement or missing bytes. New-table-only integrity and retirement triggers do not enable global legacy foreign-key activation.
- SemVer/docs: project source expectation is now `3.0.0`, classified Major because consumers adopting the immutable store must stop relying on mutable same-URL storage and distinguish blob/version/observation identities. Root README is the sole active product documentation and records migration compatibility and non-goals.
- Migration compatibility: legacy `document_blobs` remain readable and no legacy content bytes, paths, observations, or documents are moved. Startup creates the new tables/indexes/triggers only. A pre-release artifact schema with blob-level MIME remains non-destructively readable when its evidence is valid: blob MIME is ignored for identity and version MIME is backfilled from the observation. Invalid migrated provenance, lineage, references, or portable paths now fail stably on load rather than being trusted.
- TDD RED: initial implementation began with the expected `ModuleNotFoundError`; rounds one and two each added regression batches before their production fixes. Round three added one batch for the seven accepted manager findings before production changes: the exact selected run was `19 failed, 1 passed, 1 skipped`. It reproduced foreign-owner public-operation joining, all four un-serialized read APIs, cross-process legacy reference admission after deletion, effect-then-interrupt link orphaning, fallback rename effect loss, six loaded contract omissions, MIME/magic trust, global lineage cycles, and three platform gzip OS bytes. Same-thread owner reentrancy was the sole pass; secure rename was the Windows capability skip. The final round-three lifecycle residual added four per-read foreign-owner cases, explicit cross-thread rollback wakeup, and same-thread nested reads before production changes; its corrected exact RED was `5 failed, 1 passed` (only same-thread reentrancy passed).
- Focused GREEN: through the round-four residual `tests/test_immutable_artifact_store.py` is `115 passed, 17 skipped`; skips are Windows capability limits for POSIX directory-fd replacement/unlink/ancestor-error/link/rename attacks, procfs opened-context cleanup, and directory symlink creation and remain executable on POSIX. The pre-residual combined immutable/storage/document/document-manifest/prepared-execution/release/acquisition-manifest run is `292 passed, 23 skipped` plus the single expected stale-installed-metadata release failure (`1.1.0` installed versus `3.0.0` source). The dedicated #51 transaction/rollback compatibility selector remains `14 passed, 4 skipped`. Ruff format/lint pass all touched Python paths.
- Full local evidence/limitation: the final `python -m pytest tests -q --tb=short` completed at `1959 passed, 59 skipped, 464 failed`. The failure count is unchanged from the round-one baseline and remains the documented Windows/Python 3.11 native-POSIX directory-fd, `/proc/self/fd`, `O_TMPFILE`, symlink-privilege/Unix-process class plus the intentionally stale installed `web-listening==1.1.0` metadata; focused Issue #52 paths are green. `py -3.12 --version` previously confirmed no local Python 3.12 runtime, so authoritative native Python 3.12/ext4 verification remains manager-owned.
- Static/tooling evidence: Ruff format/lint and compileall pass the touched Python paths; `git diff --check` passes with only repository CRLF conversion warnings, and `web-listening --help` exits zero. Repository-wide Ruff reports six pre-existing unused import/local errors in `document_manifest.py`, `section_classifier.py`, and `tracking_report.py`, outside Issue #52 and untouched. `tests/test_release_version.py` reaches the new source `3.0.0` assertion but fails only because this unmodified host exposes stale installed `web-listening==1.1.0` metadata. No environment installation was authorized.
- Local review round four correction: retention transfers the canonical leaf into `candidate` with the existing platform atomic no-replace primitive in both secure directory-fd and fallback paths. A candidate-name race cannot be overwritten; an effect-then-`BaseException` is identity-reconciled, and a post-effect target victim is moved no-replace to the private quarantine before the original is restored, preserving both the victim and primary exception without leaving the blob row on replacement bytes. Final URL and explicit filename extensions are validated independently before mutation; exact filename evidence is stored in an additive observation column, replay-validated, and migration-defaulted to empty for old artifact tables. Fallback exclusive leaf creation now retains the opened descriptor and resolves its actual opened path through the parent postcheck; failure closes the descriptor and identity-cleans only the owned actual-context leaf, preserving a lexical replacement. Write and load validation enforce the frozen #48 limits of 64 adapter-version characters, 2048 characters for every URL including redirect/discovery evidence, and 1000 derived-from members, with exact boundary acceptance.
- Round four TDD/evidence: the corrected one-batch Windows RED was exactly `16 failed, 7 passed, 3 skipped`; failures covered fallback candidate collision/effect reconciliation, final-URL contradiction after unwanted commit, absent filename persistence/migration/load validation, an open temporary descriptor/file leak, all over-bound write/load strings, and over-bound derived lineage. Secure transfer and actual-parent POSIX cases were the capability skips. The post-format round selector is `26 passed, 3 skipped, 99 deselected`; full immutable is `113 passed, 15 skipped`; combined broader coverage is `292 passed, 23 skipped` plus only the known stale installed-version failure; #51 compatibility remains `14 passed, 4 skipped, 63 deselected`. Ruff lint/format, `py_compile`, and `git diff --check` pass the touched paths, with only repository CRLF conversion warnings.
- Round four opened-context residual: a forced fallback could follow a parent symlink race and exclusively create its temporary inode outside the configured root, then reject cleanup because the already-resolved actual path was outside that root. Failure cleanup now treats the invocation-created O_EXCL inode—not the lexical configured root—as its narrow authority: POSIX keeps the source descriptor open while the actual parent-local identity quarantine removes only that inode; Windows captures the handle path, closes before its no-replace identity quarantine, and never removes a mismatching lexical or actual replacement. Cleanup failure still cannot mask the original `BaseException`. The deterministic portable RED was exactly `2 failed, 2 skipped`; GREEN is `2 passed, 2 skipped, 128 deselected`, with the two symlink/procfs held-descriptor parameters reserved for native POSIX. Full immutable is `115 passed, 17 skipped`; Ruff lint/format, `py_compile`, and `git diff --check` pass, with only repository CRLF conversion warnings.
- Round five retention correction: if the quarantine candidate name is replaced between the staged blob-row delete and the second byte/identity verification, the store now commits the already-locked retirement/delete rather than rolling the row back onto replacement or missing bytes. Both an ordinary `ArtifactStoreError` and an injected `BaseException` preserve the primary error and the replacement privately while leaving the original either exactly republished from the still-held descriptor after a non-effected commit or durably retired with no blob row. Quarantine restoration itself now requires the named candidate to retain the pinned original identity before any no-replace transfer, including native directory-fd verification.
- Round five loaded-row correction: every persisted TEXT field across blob, version, observation/provenance, and lineage records is type-checked before regex, hashing, URL, encoding, JSON, or identity work. BLOB/numeric tampering now fails with stable `ArtifactStoreError("reference.type_invalid")`; global lineage and referenced-observation URL validation also reject non-TEXT rows instead of coercing them.
- Round five TDD/evidence: the exact RED selector was `12 failed, 8 passed, 2 skipped`. The final strengthened selector, including the superseded earlier final-check invariant, is `21 passed, 2 skipped`; full immutable is `135 passed, 19 skipped`; the storage/document/document-manifest/prepared-execution/acquisition-manifest compatibility set is `179 passed, 8 skipped`. Ruff format/lint, `py_compile`, and `git diff --check` pass. `tests/test_release_version.py` still has only the known host-environment failure: source expects `3.0.0` while the unmodified installed metadata reports `1.1.0`.
- Round five native ancestor residual: manager native ext4 verification supplied the RED (`test_retention_rejects_ancestor_replacement_and_preserves_both_trees` failed 3/3) showing that safe held directory fds alone did not detect replacement of their lexical names. Secure retention now keeps descriptor/identity pins for every directory from the artifact root through the blob parent and revalidates every lexical name against its pin before candidate verification, restoration, and deletion. A root, intermediate ancestor, or parent swap therefore fails stably with `blob.path_changed`; the already-staged retirement/delete is made durable under the write lock, the replacement tree is untouched, and the original remains in the displaced tree/private quarantine. Fallback performs the equivalent final canonical-byte audit before rollback and also retires a row whose lexical target is now replacement or missing bytes. Tests now cover root/ancestor/parent swaps in both secure and forced-fallback modes and require either exact readable original bytes or no row plus a retirement marker.
- Round five residual evidence: on this Windows host, the direct ancestor/pre-quarantine selector is `1 passed, 6 skipped, 152 deselected`; final full immutable is `135 passed, 24 skipped`, with the added skips representing native directory-rename/dirfd cases unavailable on Windows. Ruff format/lint, `py_compile`, and `git diff --check` pass. Authoritative ext4 execution of the now-expanded native cases remains manager-owned.
- Round five native pre-effect residual: manager native verification showed the symlinked-ancestor case correctly failed before quarantine ownership but the broad canonical audit still committed a retirement and deleted the blob row. Retention now carries an invocation-local `_QuarantineAttempt` which distinguishes exact leaf ownership acquisition and observed transfer effects from failures before any ownership/effect. Only an owned/uncertain/post-move attempt may enter canonical reconciliation; a symlink or invalid ancestor rejected before ownership rolls back the retirement marker and transaction, preserving both the exact original bytes and blob row. Once ownership was acquired, an unchanged canonical blob also rolls back, while a later swapped/missing canonical name retains the durable no-row behavior from the six ancestor-swap cases.
- Round five pre-effect tests/evidence: invalid-ancestor tests cover symlink and non-directory ancestors under forced-fallback and secure configurations and assert exact displaced bytes, unchanged blob row, and no retirement marker. The combined pre-effect/post-move selector is `2 passed, 9 skipped, 151 deselected` on Windows; full immutable is `136 passed, 26 skipped`. Ruff format/lint, `py_compile`, and `git diff --check` pass. Native secure/fallback execution remains manager-owned.
- Round six retention proof: a database commit which fails before taking effect no longer permits identity-only quarantine restoration. Recovery must prove the held descriptor bytes, named candidate identity and bytes, every pinned lexical directory, and the final canonical bytes against the same blob record. Candidate-name replacement, same-inode content mutation, or ancestor mutation makes restoration unprovable; the store then durably commits retirement/blob-row deletion with a subsequent transaction while preserving the original commit `BaseException`. The postcondition is exact readable canonical bytes plus a row, or a durable retirement with no row—never a row naming corrupt/missing bytes.
- Round six bounded replay and numeric contract: gzip replay now uses a single-member incremental `zlib` decoder, rejects truncation, concatenated members, and all trailing bytes, and caps each input chunk and total decompressed output against exact declared sizes. Stored-file and held-descriptor reads stop at declared stored size plus one byte, so malformed files cannot force unbounded storage allocation before validation. Blob entity/stored sizes and observation size must be exact `int` (never `bool`), nonnegative, and no greater than the frozen JSON-portable maximum `9,007,199,254,740,991` on write, load, and direct read; prior numeric coercion was removed. Redirect ordinals likewise require exact `int` on write and replay.
- Round six filename/docs: explicit filename evidence has the documented symmetric maximum of 255 characters on write and load. README now also documents portable size bounds and the complete single-gzip-member/no-trailing bounded replay policy.
- Round six TDD/evidence: the corrected exact RED was `12 failed, 3 passed, 1 skipped`; final focused is `15 passed, 1 skipped, 162 deselected` (the ancestor commit-mutation parameter is unavailable on Windows). Full immutable is `151 passed, 27 skipped`; the storage/document/document-manifest/prepared-execution/acquisition-manifest compatibility set remains `179 passed, 8 skipped`. Ruff format/lint, `py_compile`, and `git diff --check` pass with only repository CRLF notices.
- Round seven retention compensation: after a post-delete cleanup interruption restores the canonical name, compensation pins the exact canonical leaf plus the artifact root, every intermediate directory, and the blob parent through retirement-marker removal, blob-row reinsertion, and commit. Exact bytes, named-leaf identity, and every lexical directory identity are checked immediately before commit and audited again immediately after it. A pre-commit mutation rolls the compensation transaction back to the prior durable retirement; a post-commit audit failure starts a new atomic retirement and removes the reinserted row. The original cleanup `BaseException` remains primary, and a competing replacement is never deleted or named by a live blob row.
- Round seven migrated lineage: loaded derived lineage now rejects duplicate `related_artifact_id` members even when their ordinals are contiguous. The stable result is `ArtifactStoreError("lineage.invalid")`; validation is read-only and leaves a migrated duplicate row set intact for explicit operator repair.
- Round seven TDD/evidence: the exact pre-production selector was `7 failed, 6 skipped, 178 deselected`; failures covered forced-fallback canonical replace/truncate/ancestor mutations at both reinsertion and post-commit audit boundaries plus duplicate migrated lineage. Final selector is `5 passed, 8 skipped, 178 deselected` on Windows; the six secure cases require native POSIX directory-fd support and the two fallback ancestor renames are blocked by held Windows directory handles. The retention-focused selector is `25 passed, 24 skipped, 142 deselected`; full immutable is `156 passed, 35 skipped`; the storage/document/document-manifest/prepared-execution/acquisition-manifest compatibility set remains `179 passed, 8 skipped`. Ruff format/lint, `py_compile`, and `git diff --check` pass with only repository CRLF notices.
- Final tested-unreviewed P1 correction: retention compensation no longer clears the durable retirement guard when it provisionally reinserts the restored blob row. The row and marker commit together while the exact canonical leaf and complete directory chain remain pinned; queued independent legacy writers then acquire the released SQLite lock only to be rejected by the retirement triggers and roll back. The exact post-commit byte/name/directory audit runs while that guard remains visible. Audit failure atomically deletes the provisional row while retaining the marker; audit success uses a final transaction to clear the marker, after which no failure-capable filesystem audit remains. Thus no reference writer can observe an unguarded unaudited row, and the final state is either an exact readable row without a marker or a marker without a row, while the original cleanup `BaseException` and competing replacement remain preserved.
- Final P1 TDD/evidence: the exact pre-production queued-writer selector was `2 failed, 4 skipped, 191 deselected` on Windows. Both executable forced-fallback replace/truncate cases demonstrated the legacy document reference committing in the prior compensation commit-to-postaudit gap, followed by `retention.restore_failed` masking the original interruption; fallback ancestor and secure directory-fd parameters were platform skips. Final queued-writer selector is `2 passed, 4 skipped, 191 deselected`; the combined Round 7/final selector is `7 passed, 12 skipped, 178 deselected`. Full immutable is `158 passed, 39 skipped`; the storage/document/document-manifest/prepared-execution/acquisition-manifest compatibility set remains `179 passed, 8 skipped`. Ruff format/lint, `py_compile`, and `git diff --check` pass with only repository CRLF notices.
- Remaining work: manager/local reviewer owns native Python 3.12/ext4 execution of the secure and fallback ancestor matrix, the requested native full run and final read-only review closure, clean package-install version verification, and every commit/push/PR/lifecycle action. The implementation worker stops edits after this final tested-unreviewed handoff and remains available only for explicitly targeted reviewer fixes.
