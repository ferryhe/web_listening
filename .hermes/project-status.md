# Project Status

- Date: 2026-08-20 America/New_York.
- Project: `web_listening`.
- Branch and base: `codex/issue-48-manifest-contract-bundle` at uncommitted HEAD `08609642a2447728f935c73e108e82dc18b0b1e6` (`origin/main`). The worktree was clean at startup. Sibling repositories and other worktrees remained off-limits.
- Run type: delegated implementation worker for GitHub Issue #48, sourced by parent Epic #46 and dependent on the merged Issue #49 access-decision identity. This worker was explicitly prohibited from commit, push, PR/Issue mutation, merge, branch/worktree cleanup, and reviewer-agent delegation.
- Scope delivered: canonical `contracts/acquisition-manifest.v1/{schema.json,fixture.json,producer-confirmation.json}`; strict offline bundle validator; stable `validate-acquisition-contract --bundle-path --json` CLI and parser envelope; focused tests; README authority; CI installed-wheel smoke and dependency-metadata assertion; additive 1.3.0 version and declared `jsonschema[format-nongpl]>=4.23.0,<5.0.0` base dependency. No crawler/search/network runtime, CAS/storage, retention, manifest producer, REST/MCP migration, or consumer repository work was implemented.
- Contract identity: blob identity is the complete lowercase SHA-256. Observation identity is SHA-256 over canonical JSON containing exactly `manifest_version`, `source_run_id`, `normalized_source_identity`, and nullable artifact SHA-256; `artifact_id` is `artifact-` plus its first 24 hex characters. Derived identities bind the source artifact in a portable URN, while parent/source/discovered-from relationships bind stable artifact IDs.
- Contract coverage: the draft 2020-12 schema has exact `$id`/title/version identity, closed normative object shapes, only `source_run_id`, bounded strings/arrays, exact enums/formats/nullability, HTTP(S) URL and userinfo exclusion, redirect/access-decision evidence, retrieval/MIME/size/digest/portable URI fields, independent run/artifact statuses, and lineage. Local review round 2 added explicit `maxLength` bounds for adapter version, produced/retrieved RFC3339 strings, and MIME strings at each effective string occurrence, plus replay `maxItems: 1000` matching the artifact collection. Local review round 3 bounds both effective `size_bytes` occurrences at the portable JSON integer maximum `9_007_199_254_740_991` and governs each discovery kind: seed has no source artifact or URL; search has a URL but no parent artifact; link requires a parent artifact and URL; crawler supports coherent root and parent-discovered forms; derived binds discovery, parent, and source lineage. Any non-null discovery artifact is semantically bound to the same lineage reference and its final URL. The producer-confirmed fixture covers HTML, PDF, derived Markdown, all five discovery kinds including both crawler forms, parent-child/source lineage, allow/reject redirects, same URL with new bytes, different URLs with identical bytes/shared blob URI, and idempotent replay without production data.
- Canonical LF byte digests after local review round 3: `schema.json` = `3498f1028058ff7159bd199e652a972c58a13a01a980f7c06a5e8be06db4054c`; `fixture.json` = `3dac2f9044436bea779877818e72daf5f60346a3831c183b9fdb661c314efc91` (unchanged); `producer-confirmation.json` = `a7ac1a7ac30bb838894701b5de319cd230559076ef3fc63a045b38258e642a5a`. Literal schema and fixture trust anchors are frozen independently in validator code and tests. The narrowly scoped root `.gitattributes` forces LF only for `/contracts/acquisition-manifest.v1/*.json`; all three bundle files are UTF-8, CR-free, newline-terminated, and confirmation binds the exact repository, Issues #46/#48, contract version, producer-confirmed scope, and both governed digests.
- Fail-closed validator behavior: exact three-file directory only; regular non-symlink files; bounded file size; duplicate/corrupt/non-UTF-8/non-LF JSON rejection; literal schema/fixture trust anchors that self-resigned edits cannot replace; schema meta-validation and closed-shape/source-run identity inspection; local-fragment-only schema references with retrieval-disabled resolution and stable `schema.reference_invalid`; required URI/date-time format-checker availability; Issue #49-aligned raw/decoded sensitive material and URI-userinfo checks; canonical URLs; recomputed artifact IDs and portable digest URIs; portable `size_bytes` range checking; redirect continuity with only 301/302/303/307/308 and no HTTPS-to-HTTP hop; discovery-kind role/nullability/reference/URL coherence; source/derived inverse lineage constraints plus acyclic referential checks; run/artifact status consistency; canonical coverage and replay checks. `@` is excluded from URL authority/userinfo while remaining valid in schema path/query/fragment positions. Old `web-listening-manifest.v1`, sample/test-only scope, unknown files/shapes, and sensitive bundles cannot masquerade as the contract. Validation is read-only and repeated results are byte-stable.
- TDD RED evidence: the initial implementation focused command failed collection twice with `ModuleNotFoundError: No module named 'web_listening.blocks.acquisition_contract'`; a later semantic RED run produced exactly three intended failures before identity/redirect/status checks were implemented. Local review round 1 added all six accepted finding groups before production edits; `python -m pytest tests/test_acquisition_manifest_contract.py tests/test_acquisition_contract_cli.py tests/test_packaging_dependencies.py -q` produced `36 failed, 17 passed, 1 warning`. Local review round 2 added only schema-level bound tests first; targeted `-k 'explicitly_bounds or enforces_normative_bounds'` produced `6 failed, 46 deselected, 1 warning`. Local review round 3 added portable-size and discovery-invariant tests before production edits; targeted `-k 'size_bytes or semantic_size_limit or discovery_kind or binds_discovery'` produced `35 failed, 1 passed, 52 deselected, 1 warning`: both size defenses, all 14 schema contradictions, 13 of 14 new semantic contradictions, and all six discovery reference/URL mismatches were red, while the pre-existing lineage guard already rejected the remaining seed-artifact contradiction.
- Focused GREEN evidence after local review round 3: the exact targeted set passed `36 passed, 52 deselected`; the acquisition manifest module passed `88 passed`; and the acquisition/access-contract/CLI/version/packaging regression passed `303 passed, 1 skipped`. These host checks used local Python 3.11 and emitted only the known unknown-`asyncio_mode` pytest warning. Draft 2020-12 meta-validation, exact LF/digests, two identical successful CLI JSON runs, stable invalid-path JSON, help, and bundle no-mutation checks passed. Ruff fatal/error and formatter checks for the two round-3-touched Python files, `compileall`, and `git diff --check` passed. The fixture digest remained unchanged. Round-1 wheel METADATA evidence remains `Requires-Dist: jsonschema[format-nongpl]<5.0.0,>=4.23.0`.
- Authoritative full-suite evidence: before local review, the manager's sealed native-ext4 gate passed `2185 passed, 3 skipped, 1 warning` in 125.82 seconds. After all round-1 fixes, the manager's sealed Python 3.12.14/native-ext4/network-none/read-only-trace exact full suite passed `2212 passed, 3 skipped, 1 warning` in 129.85 seconds. After round 2, the manager built cache-free sealed LF source6 with source, `TMPDIR`, and cache on a native ext4 volume; under Python 3.12.14, network `none`, `--init`, privileged execution, and `strace -f -qq -e trace=%file`, exact `python -m pytest tests -q` passed `2218 passed, 3 skipped, 1 warning` in 131.46 seconds. After round 3, the manager built cache-free LF-sealed source7 from the base archive plus the exact diff/untracked overlay; with Python 3.12.14, source/`TMPDIR`/cache on native ext4, network `none`, `--init`, privileged execution, and `strace -f -qq -e trace=%file`, exact `python -m pytest tests -q` passed `2254 passed, 3 skipped, 1 warning` in 135.19 seconds. This round-3 manager gate is authoritative. The worker's round-2 raw-copy/overlayfs exploratory full run passed `2199` tests but had `19` environmental failures: 16 `O_TMPFILE` `ENOTSUP` failures because its `TMPDIR` was overlayfs rather than native ext4, plus three CRLF/raw-copy/read-only fixture and generator mismatches. That run and earlier worker CRLF/inode/cache runs are retained only as superseded provenance and are not blockers.
- Local reviewer gate: four fresh read-only rounds completed. Rounds 1-3 found technically valid, in-scope issues that were corrected and revalidated; round 4 returned PASS with no actionable spec, correctness, security, packaging, CI, documentation, or maintainability finding. The script-enforced local review gate is closed at `4/8`.
- Other checks: no live site/catalog/runtime test ran. Draft PR publication, the single remote-feedback window, merge, evidence comment, and cleanup remain manager-owned.
- Current file scope: `.gitattributes`, `.github/workflows/ci.yml`, `.hermes/project-status.md`, `README.md`, `pyproject.toml`, `contracts/acquisition-manifest.v1/schema.json`, `contracts/acquisition-manifest.v1/fixture.json`, `contracts/acquisition-manifest.v1/producer-confirmation.json`, `web_listening/blocks/acquisition_contract.py`, `web_listening/cli.py`, `tests/test_acquisition_manifest_contract.py`, `tests/test_acquisition_contract_cli.py`, `tests/test_packaging_dependencies.py`, and `tests/test_release_version.py`.
- Uncommitted/untracked state: the scoped tracked files above are modified and the new scoped bundle/module/tests/`.gitattributes` are untracked. No unrelated modified or untracked file was found. No Git or GitHub lifecycle action was taken.
- Blocker/decision: no implementation, verification, or local-review blocker remains. All Git/GitHub lifecycle actions remain manager-owned and have not yet been performed.
- Recommended next action: manager performs final diff/status validation, intentionally commits and pushes the scoped files, creates the Draft PR with `Closes #48`, marks it Ready, and follows the single-window remote-feedback/merge/cleanup workflow.
