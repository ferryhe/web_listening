# Project Status

- Date: 2026-05-12
- Project: web_listening
- Branch: main
- Run type: post-delivery documentation and skill hygiene.
- Scope: document how agents use the acquisition tool picker and validate the user-level managed PR skill with skill-creator guidance.
- Code changes: enriched `web_listening.blocks.acquisition_tools.acquisition_tools_catalog()` with stable tool-selection rules and picker metadata fields while preserving existing adapter, probe, built-in, optional runtime, and safety fields. Added UI/agent metadata including `recommended_when`, `not_for`, `operator_inputs`, `requires_profile_safety`, `output_contract`, `runtime_status`, and `frontend_control`. The mapping is ordinary public HTML -> `web_http`, dynamic JS -> `browser_rendered`, authorized stealth browser/CDP-like context -> `cloakbrowser`, bulk structured/site-specific scrape -> reserved `batch_python`, and discovery/feed choices -> reserved `sitemap`/`rss`.
- Docs changes: added `docs/contracts/acquisition-tools-v1.md`, added `docs/testing/fixtures/acquisition-tools-v1.sample.json`, updated README/docs index/testing smoke guidance to name API `GET /api/v1/acquisition/tools` and CLI `web-listening list-acquisition-tools --json` as the delivery picker contract, and refreshed the acquisition profile contract to reflect the optional safety-gated CloakBrowser probe adapter.
- Latest docs changes: README now includes the acquisition tool picker agent usage sequence, the project `web-listening-agent` skill points agents at `acquisition-tools.v1`, and `docs/contracts/acquisition-tools-v1.md` includes the normative agent usage flow.
- Tests: added `tests/test_acquisition_tools_contract_fixture.py` for fixture shape and runtime catalog drift checks; strengthened CLI/API catalog assertions for new frontend/agent fields.
- Verification: PR6 red-first fixture test initially failed because `docs/testing/fixtures/acquisition-tools-v1.sample.json` was missing; after implementation `.venv\Scripts\python -m pytest tests\test_acquisition_tools_contract_fixture.py -q` passed with 2 tests. Focused acquisition/API/CLI slice `.venv\Scripts\python -m pytest tests\test_acquisition_tools_contract_fixture.py tests\test_acquisition_capture.py tests\test_acquisition_profile.py tests\test_cli.py tests\test_api.py -q` passed with 95 tests. Fixture parse `.venv\Scripts\python -m json.tool docs\testing\fixtures\acquisition-tools-v1.sample.json` exited 0. CLI help `.venv\Scripts\python -m web_listening.cli list-acquisition-tools --help` exited 0. CLI JSON `.venv\Scripts\python -m web_listening.cli list-acquisition-tools --json` exited 0 and emitted the enriched `acquisition-tools.v1` catalog. Full suite `.venv\Scripts\python -m pytest tests -q` passed with 252 tests. `git diff --check` exited 0 with line-ending warnings only. Independent read-only review found no P1/P2 issues; its stale acquisition-profile doc P3 was fixed. PR #19 Copilot feedback was evaluated and addressed by adding conditional `required_when` input metadata, adding `site_key` to CloakBrowser operator inputs, and marking rendered-browser probing as an optional Playwright runtime in catalog/fixture/tests. PR #19 had no GitHub checks reported and was mergeable before merge.
- Merged PRs: #14 acquisition profile/capture contracts (`887df91f286cc7188c76865046c8251a626d4a61`), #15 capture adapters/evaluation (`f80d490df592bfa4975093aa23bb0ffa9afe2d6e`), #16 CLI/API acquisition tools (`cdc8b7ecf20414d2cf30150e115c5c5eaab49af6`), #17 workflow evidence/report/manifest output (`ca6880937e77d56e84bf9b77876579050b04d3d5`), #18 optional CloakBrowser adapter (`edb7e33a2ce5e1ff8f229459839111688fb2fc88`), and #19 delivery readiness/tool picker contract (`05f0b08ccf35ac80801c7b22740b31cefb46617a`).
- Current conclusion: the requested six-PR delivery program is complete. CLI and API now expose acquisition tool selection and probing surfaces, workflow outputs include acquisition evidence, optional CloakBrowser support is safety-gated, and `acquisition-tools.v1` is documented, fixture-backed, and ready for frontend/agent picker use.
- Skill status: user-level `managed-pr-development` was normalized to UTF-8 without BOM, validated with the skill-creator quick validator, and given `agents/openai.yaml` UI metadata.
- Next recommended action: wire the frontend to `GET /api/v1/acquisition/tools` for picker controls and `POST /api/v1/acquisition/probe` for pre-run probing.
