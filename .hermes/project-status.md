# Project Status

- Date: 2026-08-20 America/New_York.
- Project: `web_listening`.
- Branch: `codex/issue-49-access-decision-contract` at committed PR #59 HEAD `e96acb1b1fa83ee8a5a5cc12a558b80d31e20047`, tracking `origin/codex/issue-49-access-decision-contract`; the remote-feedback run began at that exact clean HEAD. Sibling repositories and every other worktree remain off-limits.
- Run type: the single delegated post-window remote-feedback worker for GitHub Issue #49 and PR #59. This worker is not authorized to fetch/refetch remote feedback, commit, push, create/update/comment on PRs or Issues, merge, delete branches, or remove worktrees.
- Scope delivered: strict `access-policy.v1`, `access-decision.v1`, and shared `access-rejection-error.v1` models; pure builders and offline strict loader; additive `validate-access-contract` CLI; canonical and negative fixtures; focused tests; README contract authority; and the additive 1.2.0 version update. No existing network, crawler, search, sitemap, download, REST, MCP, storage, cache, concurrency, pacing, or budget runtime was migrated or modified.
- Contract authority: access policy freezes canonical origin, transparent identity, fixed policy version, exact cache-key input, robots observation, freshness, diagnostic artifact digest, and the exactly revalidated existing `site-diagnostic.v1` `OriginPolicyEvidence` model. Diagnostic planning evidence remains evidence only and does not independently grant execution authority.
- Robots matrix: valid 200 evaluates the existing selected-rule model and allows or rejects; 404 allows as absent policy; 401 rejects as `robots.auth_required`; 403 rejects as `robots.forbidden`; timeout/DNS/network are retryable errors; parse failure is a non-retryable error. Every reject/error fails closed. Only allow decisions may contain final request and per-origin pacing/budget reservations; both fields are required for allow and must be JSON `null` for reject/error.
- Local-review fix 1: all three accepted round-1 P1 findings were corrected. NFKC-normalized delimiter/compact/camel secret-key checks now cover every access URL and embedded diagnostic sitemap URL, while header/credential-shaped identity, warning, and rule text is rejected without changing the diagnostic producer. Every consumed redirect request now carries a finite digest-bound `access-decision-proof.v1` allow proof with exact source URL/origin, policy/evidence, disposition, slot, and origin reservation; strict source/proof/slot/chain/no-downgrade/causal-time checks apply. Reservation windows are half-open, `not_before` remains active, and same-origin/exact-window redirect lineage cannot roll budget usage back or contradict a nonzero pacing schedule.
- Local-review fix 2: all three accepted round-2 findings were corrected. Access URL scanning now covers raw plus two percent-decoding passes with both ampersand and semicolon separators, and free text rejects whitespace-delimited credential pairs. Pacing lineage is keyed by canonical origin across budget-window changes; exact windows preserve monotonic budget lineage, differently shaped overlapping windows fail closed, and only later non-overlapping windows may reset budget usage while retaining origin pacing. Reservation timestamps and numeric fields have documented portable maxima, time arithmetic is converted to governed validation errors, and the loader wraps any residual `OverflowError` for exact CLI error-envelope serialization.
- Local-review fix 3: all three accepted round-3 findings were corrected. Raw/twice-decoded access query scanning now treats nested `?` boundaries like `&`/`;`, while the shared protocol helper rejects HTTP(S) URI userinfo embedded anywhere in access identity/evidence free text after NFKC, backslash, and special-scheme normalization. Every allow reservation binds `not_before` to policy expiry, and every consumed redirect request must begin at/after `not_before`, inside its reservation's half-open budget window, and within policy authority. `validate-access-contract` is now JSON-aware at the command-group parser boundary, so JSON-mode missing/invalid/unreadable paths and handler validation failures share one exact compact `contract.invalid` envelope while human parser diagnostics remain unchanged.
- Local-review fix 4: all three accepted round-4 findings were corrected. Every raw/twice-decoded query inspection copy is NFKC-normalized before secret-key tokenization, closing raw and encoded fullwidth delimiter/key forms. Credential scanning restarts after assignment and true punctuation boundaries, so nested identity, warning, and selected-rule tokens fail after complete digest re-signing. Access query canonicalization preserves parameter order and reserved encodings while requiring visible RFC 3986 query ASCII, well-formed uppercase percent escapes, literal unreserved bytes, and exact submitted equality; malformed escapes, raw whitespace/control/non-ASCII, lowercase escapes, and encoded-unreserved aliases fail closed.
- Local-review fix 5: the accepted round-5 P1 was corrected with fixed raw/once-decoded/twice-decoded NFKC inspection that checks every HTTP(S) scheme start in linear time. A signed outer URL can no longer hide raw, percent-encoded, or double-percent-encoded nested URI userinfo; the same access-layer check covers identity, embedded warning/rule evidence, and sitemap URLs while preserving benign nested URLs and leaving generic Site Skill/site-diagnostic validation unchanged. Fully re-signed decision/policy negative fixtures and exact secret-free CLI envelope tests cover the repaired path.
- Local-review fix 6: both accepted round-6 P1 findings were corrected. The same bounded raw/once-decoded/twice-decoded linear authority scan now covers HTTP(S), the SOCKS scheme family, and boundary-qualified network paths while retaining RFC-valid userinfo punctuation and honoring genuine authority terminators; outer-re-signed decision/policy fixtures cover the repaired URL, identity, warning, and rule paths without changing generic protocol behavior. `access-rejection-error.v1` is now independently loadable and binds typed non-sensitive digest-derived evidence IDs, recomputable cache keys, zero-to-24-hour ordered freshness, the all-null/all-present origin-policy reference trio, embedded robots observation, and the exact reason/outcome/retryability/evidence matrix.
- Local-review fix 7: the accepted round-7 P1 was corrected. Access identity, warnings, selected rule patterns, and containing decisions now inspect fixed raw/once-percent-decoded/twice-percent-decoded NFKC free-text passes for header and credential-key forms. The unexplained 80-character key cap was removed; complete token matching remains boundary-qualified and linear, with fully re-signed once/twice-encoded and long HTTP-token regressions, benign controls, a 100k/200k/400k scaling check, a digest-valid negative fixture, and the exact secret-free CLI error envelope.
- Final unreviewed fix after round 8: all three accepted findings were corrected under the explicit no-round-9 instruction. Every frozen compact exact secret key now participates in namespaced suffix detection across the same raw/once/twice URL and free-text passes. Network-path userinfo recognition pairs RFC-invalid `|`, `^`, and backtick as both candidate-start boundaries and authority terminators, while in-token paths and schemes remain benign. Excessive JSON nesting from the duplicate-key preparse is a governed model `ValueError`, loader `AccessContractError`, and exact single-line JSON CLI `contract.invalid` envelope. Fully re-signed policy/decision fixtures cover namespaced secrets and pipe-delimited authorities.
- Linux parser-portability correction: the two excessive-nesting tests now assert the governed model/loader exception boundaries instead of CPython- or pydantic-core-specific diagnostic text. The exact public JSON CLI envelope remains separately frozen and secret-free; production fail-closed behavior was not weakened.
- Remote-feedback fix: all five manager-supplied snapshot items were confirmed valid and in scope. The example `tenantapikey` in the first comment was not itself a regression because the pre-existing generic suffix set already rejected it, but the newly added `privatekey`/proxy exact-name suffixes did unintentionally widen generic Capture/Site Skill portable JSON validation. Generic behavior is restored while access validation explicitly opts into the stricter namespaced exact-name suffixes. Access URL inspection now applies the fixed raw/once/twice-decoded NFKC text scan to the complete URL, closing secret-bearing path and query-value forms. Robots wildcard evaluation was replaced with ordered segment matching that preserves the selected-rule semantics without regex backtracking. Human validation of a standalone `access-rejection-error.v1` omits unavailable ID/digest rows. This status file now distinguishes superseded exploratory results from the authoritative pre-PR and post-feedback gates.
- Security and determinism: URL/origin/redirect/query canonicalization, no HTTPS downgrade, no URI userinfo—including overlapping raw/percent/double-percent HTTP/SOCKS/network-path authorities and pipe/text-punctuation-delimited network paths—or raw/encoded/nested/fullwidth-delimiter/namespaced secret-like query keys and secret-bearing path/query values, no raw/once/twice-encoded header or credential-key data (including long tokens) and no nested delimiter- or whitespace-shaped secret data in identity or embedded evidence, linear-time robots wildcard matching, exact evidence references, independently strict typed non-sensitive error evidence, duplicate-key and excessive-nesting rejection, strict unknown/required/null/enum validation, canonical JSON, recomputable identity/cache/policy/decision/proof digests, contiguous redirect/request-slot and per-origin budget/pacing lineage, non-overlapping window transitions, portable arithmetic, policy/reservation/request temporal authority, freshness, idempotence, and no-mutation validation are covered.
- SemVer decision: Issue #49 is an additive contract and offline CLI capability, so project authority advances from 1.1.0 to 1.2.0. A later complete migration of existing crawler/search/sitemap/download/API/MCP entrypoints to the new gateway changes authority semantics and requires 2.0.0.
- Files in scope: `.hermes/project-status.md`, `README.md`, `pyproject.toml`, `web_listening/cli.py`, `web_listening/contracts/__init__.py`, `web_listening/contracts/_protocol.py`, `web_listening/contracts/access_decision.py`, `web_listening/contracts/site_diagnostic.py`, `web_listening/blocks/access_contract.py`, twenty-eight `docs/testing/fixtures/access-*` JSON fixtures, `tests/test_access_decision.py`, `tests/test_access_decision_cli.py`, `tests/test_site_skill_protocol_contracts.py`, and `tests/test_release_version.py`.
- Authoritative pre-PR verification: the manager's corrected combined access/site-diagnostic/CLI/version regression was `449 passed, 1 skipped`, and its clean Linux 3.12.14 native-ext4 full gate was `2145 passed, 3 skipped, 1 warning`. Those results supersede the earlier parser-portability, anonymous-volume, and Windows exploratory failure counts retained in prior status revisions.
- Remote-feedback RED/GREEN: the four full-component secret URL cases, adversarial robots matcher, standalone human display, and four shared-protocol suffix controls first failed exactly as reported (`10 failed`). After the minimal fixes, that feedback subset passed (`23 passed`), and access contract/fixture/CLI/version tests passed on supported Windows Python 3.12.12 (`203 passed, 1 skipped`). A 93,654-case deterministic comparison found the new robots matcher semantically identical to the old expression matcher; 10k/20k/40k adversarial inputs completed in approximately `0.0007/0.0012/0.0024` seconds. The wider access/site-diagnostic/Capture/Site Skill/CLI regression passed on normalized Linux (`882 passed, 1 skipped`); its Windows run had `878 passed, 1 skipped` plus exactly four existing platform failures (fixture CRLF digest, fake `/bin/sh` BrowserAct, and two terminal-width help assertions). The final Windows access/CLI/release/shared-protocol selection passed `545 passed, 1 skipped, 1 deselected` (the deselection is that known CRLF byte-digest assertion). Focused Ruff fatal/error checks passed with only the two documented CLI F541 rules excluded, formatter checks passed for every touched file that was formatter-clean at the committed HEAD, and the touched hunks in the three pre-existing whole-file formatting baselines require no additional formatter change. `compileall` and `git diff --check` passed.
- Full offline regression after remote fixes: Windows Python 3.12.12 completed with `1649 passed, 477 failed, 33 skipped, 1 warning`; the failure count is unchanged from the known Windows/POSIX baseline, while all eleven new feedback cases are additional passes. A fresh privileged Python 3.12.14 container with source, virtual environment, and pytest temporary directory on native ext4 completed with `2156 passed, 3 skipped, 1 warning`. No live-network test or external catalog command was run; the disposable container, loop mount, and temporary archives were removed and verified absent.
- Reviewer/remote state: the manager-supplied one-time snapshot reported PR #59 HEAD `e96acb1`, CI test success, Copilot check success, and mergeable/clean, with no other human or Issue feedback. This worker did not access the remote. The explicitly permitted final unreviewed fix after local round 8 remains closed with no round 9; the remote-feedback fixes are uncommitted for manager inspection and validation.
- Uncommitted/untracked state: eight tracked files are modified by this remote-feedback run (`.hermes/project-status.md`, three implementation modules plus `web_listening/cli.py`, and three test modules); there are no untracked files and no unrelated path was found.
- Next recommended action: the manager inspects this exact remote-feedback diff, reruns any desired local evidence, records the audited remote fix, intentionally commits and pushes the update without restarting the wait or refetching feedback, waits only for required checks, then proceeds with the authorized merge/cleanup lifecycle if green.
