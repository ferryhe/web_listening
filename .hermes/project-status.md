# Project Status

- Date: 2026-08-07
- Project: `web_listening`
- Branch: `codex/robots-sitemap-plan-prerequisite`
- Run type: managed documentation reprioritization.
- Baseline: clean synchronized `main` at `f0d5d94`, including merged PR #41; sibling repositories are off-limits.
- GitHub inventory: there are no open pull requests. Before this change, the merged Agentic Site Monitoring plan had three unimplemented in-repo PRs (PR1-PR3) plus one downstream integration PR outside this repository; after adding PR0, the current plan has four unimplemented in-repo PRs (PR0-PR3) plus that one downstream PR.
- Scope: add a new in-repo PR0 ahead of PR1-PR3 so every new Agentic planning path first diagnoses `robots.txt`, derives declared or deterministic fallback sitemap evidence, and only then proposes a bounded acquisition/discovery plan.
- Files in scope: `.hermes/plans/agentic-site-monitoring.md` and this status file only.
- Current state: the plan now defines `site-diagnostic.v1`, robots-first request ordering, strict actual User-Agent/product-token identity binding, RFC 9309-compatible fixtures, exact document origins/ports, cross-origin sitemap robots preflight, deterministic sitemap retry/FIFO rules, streaming wire/decoded budgets with bounded decompression, deterministic orthogonal status/recommendation rules, canonical-origin page seeds, per-request DNS/IP-pinned SSRF defenses, XML/gzip protections, artifact digest/freshness/lineage, and consumption by PR1 API, PR2 UI, PR3 maintenance, and later downstream integration. PR1 resolves immutable operator review only by `review_id` from an operator-written trusted append-only store, carries a per-normalized-origin `origin_authority_bindings` collection through every planning/execution artifact, and reapplies the matching approved robots policy before every sitemap/page/file request. PR3 maintenance requests receive a server-generated immutable snapshot of the existing reviewed scope/profile authority and may only tighten its still-valid intersection.
- Authority decision: robots, sitemap, and diagnosis artifacts remain planning evidence only. They do not grant legal or execution authority, cannot expand allowed domains, cannot bypass robots rules, and cannot replace operator review, scope/profile/Site Skill bindings, or the compiled execution plan.
- Compatibility decision: proposed `diagnose-site` and `site-diagnostic.v1` are future additive PR0 deliverables; any optional paired `discover --diagnostic-id`/`--diagnostic-review-id` consumer belongs to PR1 and must resolve authority through the trusted store. Each implementation requires its own Minor SemVer decision and README/schema updates; the current 1.0 README and reserved sitemap adapter remain authoritative until then.
- Verification: final focused verification passed after all accepted reviewer fixes: `git diff --check`, current root/discover/probe CLI help, machine-readable acquisition-tool inspection, and the focused acquisition-tools contract fixture passed (2 tests); the current sitemap adapter remains correctly marked `reserved` and non-probe-capable, heading inspection confirms PR0 precedes PR1-PR3, and contract audits found no missing required fields or stale status names. Local Python 3.11 emits the known pytest `asyncio_mode` configuration warning; it does not fail the documentation checks.
- Reviewer gates: every valid finding from the first six rounds was accepted and addressed. The final seventh-round gates are clean against latest `origin/main=f0d5d94`: fresh spec PASS and fresh quality/security APPROVED, both with no Critical, Important, or Minor findings. They confirmed the robots-first ordering, homogeneous multi-origin identity, complete authority lineage, and identical PR0/PR1 SSRF timing with pre-connect rejection plus post-connect/pre-HTTP TLS/peer rejection.
- Managed-program heartbeat: active through local verification, reviewer gates, PR publication, the required remote feedback window, merge, and branch cleanup.
- Uncommitted/untracked state: only the scoped plan change and this status update are pending; no unrelated or untracked files are present.
- Next recommended action: commit/push/create the gated documentation PR, wait at least 10 minutes, and merge only when CI and all valid feedback are clean.
