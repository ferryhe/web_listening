# Project Status

- Date: 2026-08-08 America/New_York.
- Project: `web_listening`.
- Branch: `codex/robots-sitemap-diagnostic`.
- Baseline: clean synchronized `main`/`origin/main` at `536f6eec`; sibling repositories remain off-limits.
- Run type: managed PR0 implementation for the robots-first prerequisite in `.hermes/plans/agentic-site-monitoring.md`.
- GitHub inventory: implementation PR `#44` (`https://github.com/ferryhe/web_listening/pull/44`) is open and ready from `codex/robots-sitemap-diagnostic` to `main`; remote CI is green. Copilot's two in-scope comments were accepted and fixed: `--json` now emits the contract's exact canonical JSON, and the unreachable pre-TLS `ssl.SSLError` branch was removed.
- Scope: add the producer-only `diagnose-site` CLI and stable `site-diagnostic.v1` evidence contract. The diagnosis normalizes the requested authority, requests canonical `/robots.txt` first, applies the fixed identity and RFC 9309-compatible selected policy, follows only exact-approved document origins, performs auxiliary-origin robots preflight, processes bounded sitemap XML FIFO, and never fetches page `<loc>` values.
- Authority boundary: the diagnostic artifact is planning evidence only. It does not grant acquisition authority, create operator review, change `discover`, alter scope/profile/Site Skill authority, expose REST/MCP consumers, or activate the reserved sitemap acquisition adapter. PR1 remains responsible for the independent digest-bound operator review and trusted diagnosis consumer.
- Security and determinism: every request revalidates exact origin/port, public all-address DNS, pinned connection peer, canonical Host, and HTTPS SNI/certificate before application bytes. Bodies use raw wire/decoded budgets, bounded single-member gzip, strict UTF-8 sitemap XML, and fail-closed DTD/entity/XInclude/root/namespace handling. Per-origin policy evidence includes visible ordered selected rules with a recomputable policy ID/digest. Artifact reads are strict, reject duplicate JSON keys and malicious re-signed authority/identity/budget mutations, and verify the canonical artifact digest. Writes are atomic, no-overwrite, and idempotent for byte-identical artifacts.
- Files in scope: `README.md`, `pyproject.toml`, `web_listening/cli.py`, `web_listening/blocks/site_diagnostic.py`, `web_listening/contracts/site_diagnostic.py`, `web_listening/contracts/__init__.py`, `docs/testing/fixtures/site-diagnostic-v1.sample.json`, `tests/test_site_diagnostic.py`, `tests/test_site_diagnostic_cli.py`, `tests/test_release_version.py`, and this status file.
- Version decision: additive Minor SemVer update from `1.0.1` to `1.1.0`; the local editable package metadata was refreshed to `1.1.0` for verification.
- Focused verification: after the Copilot fixes, PR0 service/contract/CLI/acquisition-fixture tests pass (`256 passed`), including an exact canonical stdout assertion; the committed fixture validates its schema, policy digest, artifact digest, request slots, counted occurrences, policy ordering, structured outcome reasons, primary-disposition timeline, duplicate-decision proof, structured policy-preflight outcomes, and exact redirect targets. Focused Ruff for the new service/contract/tests passes, `compileall` passes, `git diff --check` passes, and both `diagnose-site --help` and unchanged `discover --help` render successfully.
- Regression verification: after the Copilot fixes, the core CLI/discovery/classification/scope/acquisition/diagnostic/version set passes (`505 passed`, one known POSIX-shell BrowserAct test deselected). An expanded Windows run recorded `773 passed, 360 failed, 4 skipped`; the failures are dominated by the existing POSIX-only `os.O_DIRECTORY`/directory-descriptor Site Skill registry implementation and `/bin/sh` fixtures. The earlier complete unsupported Windows/Python 3.11 run recorded `1257 passed, 476 failed, 32 skipped` for the same platform baselines. Project Python support remains `>=3.12,<3.13`.
- Static baseline: a full Ruff scan of `web_listening/cli.py` reports two pre-existing F541 findings at lines 541 and 597 (unrelated no-placeholder f-strings). They are not changed in this focused PR; the new and touched P0 code is clean.
- Reviewer gates: clean. Earlier rounds found and drove fixes across policy/protocol/authority, hard budgets, bidirectional lineage, parsing, evidence/status/priority, redirects, deterministic provenance, raw evidence, re-signed contracts, exact retries/statuses, auxiliary ordering, IPv6, cap proofs, FIFO, outcome reasons, duplicate causality/precedence, queue continuity, exact preflight/redirect targets, multi-root handling, priority precedence, redirect cardinality, and post-attempt budget closure. On the final stable worktree, R25 spec returned `PASS` with no findings and the final user-scoped incremental quality/security review returned `APPROVED` (`19 passed`, `231 deselected`) with no findings. Per the user's explicit instruction after more than five review rounds, no additional reviewer rounds will be added and reviews remain scoped to modified areas.
- Uncommitted/untracked state: only the scoped Copilot follow-up fixes, their CLI test, and this status update are present; no unrelated files were found.
- Next recommended action: push the focused Copilot fixes, complete the renewed remote CI/Copilot/comment observation window for PR `#44`, then merge it, delete the completed task branch, and resynchronize local `main` when clean.
