# Project Status

- Date: 2026-08-21 America/New_York.
- Project/worktree: `web_listening` at `C:\Project\web_listening\.worktrees\issue-51`.
- Branch/base: `codex/issue-51-unified-read-paths` from exact clean `origin/main` baseline `91af98cbf57d8f50eaa29b5f354750a562ce0a08`.
- Boundary: delegated implementation worker for GitHub Issue #51 only. Sibling repositories, sibling worktrees, parent Epic links, every other Issue, and manager state `.git/codex-managed-pr-state/issue-51.json` remained off-limits. No subagent, network/live canary, commit, push, PR/Issue mutation, merge, branch deletion, or worktree cleanup occurred.
- Delivered behavior: supported HTML/XML/feed/sitemap, PDF/attachment/document, crawler/tree, candidate re-entry, and staged `web_http` target reads now consume bounded bytes through the Issue #50 `AccessGateway`. Redirects remain manual and every hop is independently authorized. Direct browser/Playwright/CloakBrowser/BrowserAct navigation is disabled. Legacy tree, CLI site check/download, and REST site check/download/rescue target-read entrypoints reject before Storage. Offline `httpx.MockTransport` compatibility is explicitly test-only and still wraps the gateway.
- Formal authority: reviewed scope limits cannot be enlarged; complete scope bindings, strict acquisition profile, resolved Site Skill, effective runtime limits, and a non-empty compiled `web_http` plan are loaded and compiled exactly once into one frozen `PreparedScopeExecution`. That exact plan/gateway/target/execution-seed is handed unchanged to staged/tree execution; API job persistence is deferred until successful completion. Scope/profile files are never reloaded after preparation, including bootstrap summary generation. CLI, REST, and MCP share this preparation. Missing/partial authority and identity overrides fail closed before Storage.
- Pre-admission/write ordering: the prepared gateway admits the exact seed before constructing/opening `Storage` or creating any job/run/site/scope/report/temp/blob/content/bookkeeping state. Robots 401, 403, valid disallow, and transport error make only the robots request, invoke no target consumer, and preserve existing database bytes. The exact ordered/query-bearing seed URL is consumed once by the tree without sanitization, a second target request, or a second budget unit; its canonical form is used only for deduplication. All current-execution database changes and newly created governed blob/tracked/acquisition-artifact files remain transactionally pending. A propagated access rejection/error at any later page or file rolls them back and preserves preexisting rows, byte-identical database state, reports, downloads, and interface job state. Cancellation/BaseException also rolls back, successful completion commits the outer transaction, and an ordinary non-access execution failure retains the existing recorded-failure policy. Gateway ownership is closed on preparation failure, staged failure, and every API/MCP handoff exit; close is idempotent.
- Interface contract: robots reject/error propagates as the exact frozen `access-rejection-error.v1` payload through CLI/API/MCP with the same reason code. Browser/subprocess and legacy compatibility errors do not invent new frozen access reason codes.
- Version/docs: project version and release test now require `2.0.0`. Root `README.md` is the sole active product authority and contains the authoritative interface/read-path inventory, disabled/isolated paths, formal authority requirements, interface rejection behavior, and Major SemVer decision.
- Live canary: `tests/live/test_authorized_access_gateway_canary.py` requires three explicit authorization environment variables, records credential-free target/window/time/result JSON, and skips offline by default. It was collected only as a skipped test; no target authorization was supplied and no network canary ran.
- TDD RED evidence: the QA-focused first run was exactly `8 failed`: four staged robots outcomes wrote/swallowed rather than rejecting, the allowed path opened DB before admission, API compiled twice and changed files altered execution, API created a job before reject, and MCP returned a job-style error after mutation. A later summary-authority test failed `1 failed` with HTTP 500 because the changed scope file was reloaded after execution. The native Python 3.12 full gate then reported `2321 passed, 4 skipped, 1 failed`: the sole stale rescue test invoked `run_site_rescue` without a gateway and correctly received the production fail-closed `RuntimeError`. The isolated local RED reproduced exactly `1 failed`. Earlier Issue #51 RED evidence remains: missing unified reader, legacy interface envelope, Storage-reaching legacy tree/CLI/API paths, missing explicit authority/canary, limit enlargement, and version 1.4.0.
- Focused GREEN evidence: prepared execution is `8 passed`; prepared plus unified interface/static coverage is `21 passed`; staged authority is `30 passed`; bootstrap summary plus prepared authority is `10 passed`; static bypass/interface/prepared selection is `10 passed`. The final gateway/crawler/document/acquisition/staged/tree supported-host matrix is `412 passed`. The rescue contract now explicitly supplies a governed read gateway when verifying the real site ID and separately proves a missing gateway raises before `Crawler` construction: rescue is `3 passed`, and rescue plus unified/static is `16 passed`. These tests independently prove zero `Storage` construction and byte-identical DB/no artifact state on robots rejection, single compilation, effective-limit sealing, changed-file immunity, identical gateway handoff, and one target request for the admitted seed.
- Broader/interface evidence: a broader gateway/plan/lineage/tree run produced `497 passed, 64 failed`; every failure was the known unsupported Windows/Python 3.11 native-POSIX descriptor/symlink class (`os.O_DIRECTORY`, directory `os.open`, `/proc/self/fd`, or symlink privilege), not a touched-path behavior regression. API paths outside the 26 POSIX preview selections pass `33 passed, 26 deselected`; MCP passes `33 passed, 1 deselected` with only the async registration test excluded because this host lacks `pytest-asyncio`; CLI passes `64 passed, 1 deselected`, excluding only its POSIX-shell BrowserAct fixture. The earlier unsupported local full run was `515 passed, 80 failed, 4 skipped`, with the same native-POSIX classification. Per controller direction no additional long Windows full run was started. The repository declares Python 3.12 and the manager owns the required clean native-ext4/Python 3.12 full suite.
- Version evidence limitation: `tests/test_release_version.py` now passes the source expectation for `2.0.0` but the unsupported host has stale installed `web-listening==1.1.0` metadata. No environment install was authorized. The manager's clean package install is required for authoritative version GREEN.
- Static/smoke evidence: supported-path static bypass audit passes; repository search finds no direct `httpx`/requests/browser navigation in production read paths and only the explicit offline mock adapter uses `follow_redirects=False`. CLI root help and offline access-decision `--json` validation pass. API and MCP interface tests pass. Ruff lint passes for every touched Python file; all four new Python files pass Ruff format; compileall and `git diff --check` pass. No live/network command was run.
- Local review round 1 corrections: canonical `discover` now constructs one digest-bound planning `AccessGateway` per reviewed catalog target and propagates robots/gateway failures before writing inventory or report artifacts. Formal compiled `web_http` execution validates one complete, positive, consistent `(timeout_seconds, stdout_bytes)` pair and passes those exact values to the pinned transport and bounded body reader; the prior 30-second/64-MiB runtime constants are gone. Tree bootstrap and `TreeCrawler` require an accepted exact-URL seed before any bookkeeping call, and the legacy incremental wrapper fails before catalog or Storage access. CLI bootstrap/run each call `prepare_scope_execution` once, pass the sealed object into staged execution, and use `artifacts.plan`; there is no preliminary scope reload.
- Round 1 TDD evidence: initial `tests/test_review_round1_authority.py` RED was exactly `8 failed, 1 passed`; the eight failures independently exposed discovery allow/reject, pre-Storage seed validation, byte-unchanged rejection, legacy incremental disablement, missing sealed limit validation, and missing transport timeout handoff. CLI single-preparation RED was exactly `2 failed, 74 deselected`. GREEN is `12 passed, 65 deselected` for the round-specific tests and `93 passed` for review-round/staged/tree/prepared/unified coverage. The tests prove real offline discovery makes exactly robots plus one target request on allow, robots-only on 401/403, creates no rejected discovery artifact, leaves database bytes unchanged for an unaccepted seed, reuses the admitted response, derives exact body/timeout limits, rejects oversized bodies before returning a consumer result, and ignores scope/profile file changes after CLI preparation.
- Round 1 broader evidence: access/core is `296 passed`; API excluding the host-incompatible POSIX preview-input selections is `33 passed, 26 deselected`; MCP excluding the async registration test unavailable without `pytest-asyncio` is `33 passed, 1 deselected`; CLI excluding its POSIX-shell BrowserAct fixture is `66 passed, 1 deselected`; unified static/interface is `13 passed`. The combined affected run was `158 passed, 1 failed`; its sole failure was that same Windows/Python 3.11 POSIX-shell BrowserAct fixture. A broader core+API run was `331 passed, 24 failed`, with all 24 failures in the previously documented native-POSIX descriptor/symlink class. No network/live canary ran.
- Round 1 static/tooling evidence: the production direct-HTTP/auto-redirect/browser-navigation search returned no matches; Ruff lint and Ruff format check pass all 30 changed/untracked Python files; compileall, `git diff --check`, CLI root `--help`, and offline CLI `--json` envelope smoke pass. Root README now explicitly inventories governed planning discovery, exact-seed tree admission, disabled incremental compatibility execution, sealed transport/body limits, and CLI single preparation.
- Local review round 2 correction: `AccessGateway.request_with_context` now gives an opted-in body consumer one frozen gateway-owned `AccessGatewayConsumerContext` containing the canonical final-hop URL. The existing `request(..., consume=raw_only)` API is unchanged and adapts internally to the same request loop. `GovernedReadGateway` uses the context URL for bounded-body suffix/compression validation, so a cross-origin `.xml.gz` to `.xml` redirect accepts plain XML while `.xml` to `.xml.gz` rejects a plain body with `gzip_signal_mismatch`. Authorization, manual redirect, reservation/budget, consumer-error, and response-close behavior are unchanged.
- Round 2 TDD/evidence: the two real cross-origin governed-read regressions first failed exactly `2 failed, 13 deselected`, in opposite directions, and the direct context API test separately failed exactly `1 failed` because the context method did not exist. GREEN is `3 passed, 13 deselected` for those tests, full unified/static/interface is `15 passed`, the explicit static bypass check is `1 passed`, and the direct context test is `1 passed`. The complete raw-consumer gateway suite is `47 passed`; gateway/access/unified/document/crawler/tree/prepared coverage is `296 passed`; supported API/MCP/CLI coverage is `132 passed, 28 deselected`, with only the already documented unsupported Windows/Python 3.11 POSIX preview, absent async plugin, and POSIX-shell BrowserAct selections omitted. The production call-site audit finds exactly one body consumer (`GovernedReadGateway`); all other `consume=` calls are raw-only gateway tests and pass unchanged. Ruff lint/format check passes all 32 changed Python paths; compileall, root CLI help, and `git diff --check` pass. No network/live canary ran.
- Local review round 3 correction: `PreparedScopeExecution` now freezes and validates the unchanged exact execution seed before any Storage construction. Tree bootstrap/run uses it unchanged for the first admitted-response acquisition while retaining a separate canonical dedupe key. `Storage` supplies a nested execution transaction that suppresses method-level commits and journals only newly created governed files; access rejection/error rolls back the current execution, created files, and empty directories without touching preexisting paths. Scope API jobs are buffered in memory and persisted once only after success, while generic non-access failures retain a final failed job record.
- Round 3 TDD/evidence: the isolated RED was exactly `7 failed, 8 deselected`: the ordered/tracking query seed bootstrap returned `failed`, and CLI/API/MCP page/file late rejections changed rows and exact database bytes. Every rejection already had the frozen `robots.disallowed` envelope and made only robots + allowed-seed requests. GREEN is `12 passed, 8 deselected` after expanding the matrix to include an allowed document write followed by a denied page, nested commit suppression, exact byte rollback, preexisting-file preservation, successful outer commit, and cancellation rollback; the complete prepared file is `20 passed`. Tree is `32 passed`; storage/document/tree/staged/prepared affected coverage is `139 passed`; access/unified/crawler/rescue coverage is `100 passed`; supported API is `33 passed, 26 deselected`, CLI is `66 passed, 1 deselected`, and MCP is `33 passed, 1 deselected`. The exclusions remain only the previously documented unsupported Windows/Python 3.11 POSIX descriptor/symlink, POSIX-shell BrowserAct, and absent async-plugin cases. Static direct-read search returned no production match. No network/live canary ran.
- Pre-review native publication correction: `Storage.admit_inline_acquisition_artifacts` now treats `KeyboardInterrupt`, `SystemExit`, and every other `BaseException` as rollback conditions at both descriptor staging and the main savepoint/publication boundary. It records a newly linked inode before verification, closes every opened unnamed descriptor, rolls back database state, unlinks only the identity-matching invocation-owned file, and removes only identity-matching invocation-created empty directories while preserving and re-raising the original exception. A replay interruption cannot remove or replace an already-published exact inode. Three native regressions cover post-link `KeyboardInterrupt`, pre-staging `SystemExit`, and preexisting-inode preservation. The manager supplied the native ext4 RED (database rollback but one named screenshot leak) and then verified all three regressions GREEN (`3 passed`) plus the broader native matrix (`506 passed, 1 skipped`). This Windows/Python 3.11 host reports the new selector as `3 skipped, 88 deselected` because `O_TMPFILE` is unavailable; its supported local evidence is `39 passed` for storage/document/prepared and `33 passed` for unified-static/acquisition-capture.
- Pre-review format correction: project Ruff `0.15.12` reformatted only `tests/test_acquisition_lineage.py`; no manual or semantic test edit was made. Ruff format check reports all `35` touched/new Python files formatted, Ruff lint passes the same paths, and compileall plus `git diff --check` pass.
- Local review round 4 correction: inline O_TMPFILE publication now establishes pending target/source-inode ownership before entering the link call, then transfers it only after a clean return; an effect-then-`BaseException` is therefore identity-cleaned without touching a preexisting or replaced inode. Canonical blob and tracked-view publication share one exclusive hardlink/copy-temp-to-hardlink helper that retains local identity ownership until `Storage` journal registration returns, including exceptions raised after either publication or journal append. Execution commit exceptions now use SQLite `in_transaction` as the effect boundary: a pending transaction is rolled back with its owned files, while an already-durable transaction clears depth/journal ownership and keeps durable files, before the identical primary exception is re-raised. Cleanup failures cannot mask that primary exception.
- Round 4 TDD/evidence: portable RED was exactly `5 failed` (two commit pre/post-effect state failures, tracked hardlink and copy effect leaks, and canonical blob hardlink effect leak). A robustness RED then failed exactly `1` because canonical temporary-file cleanup masked the primary publication interruption. The focused portable matrix is now `19 passed` across commit pre/post effect, real tree outer commit, cancellation, cleanup-error masking, hardlink/copy effect interruption, journal-handoff interruption, and preexisting/replacement inode safety. Full document/prepared/tree is `73 passed`; storage/staged/unified/access/crawler is `161 passed`; supported API is `33 passed, 26 deselected`, CLI is `66 passed, 1 deselected`, and MCP is `33 passed, 1 deselected`. The explicit static bypass test is `1 passed` and its production search has no matches. Four new O_TMPFILE/standalone-commit cases are native-ext4-capable and report `4 skipped, 91 deselected` on this Windows host; the manager owns their native Python 3.12 execution.
- Local review round 5 correction: Storage journal handoff now accepts the publisher's expected `(st_dev, st_ino)` and verifies that exact regular no-follow leaf before claiming it. Paths are lexically anchored below the cleanup root without resolving the leaf; native hosts traverse every parent through `O_DIRECTORY|O_NOFOLLOW` dir-fds and use `stat(..., follow_symlinks=False)`, while the Windows fallback lstat-checks every parent and leaf. Shared tracked/blob publication and nested inline O_TMPFILE publication pass their created inode identities. Any replacement, symlink, traversal, or identity mismatch raises `ExecutionArtifactOwnershipError`; publishers retain local ownership and remove only their own inode, and tree execution treats the mismatch as a rollback-required control failure rather than committing a generic failed run. Journal rollback uses the same anchored identity removal and preserves post-handoff replacements and victims.
- Round 5 TDD/evidence: initial portable RED was `2 failed, 1 skipped, 25 deselected` for direct expected-identity/traversal/symlink registration plus an independent shared-publisher `1 failed`; the API and identity handoff did not exist. GREEN is `3 passed, 2 skipped, 25 deselected` for journal mismatch/traversal/rollback safety and `8 passed, 4 deselected` for all tracked-view publication/handoff cases. Storage/document/prepared/tree is `92 passed, 2 skipped`; staged/unified/access/crawler is `147 passed`; supported API is `33 passed, 26 deselected`, CLI is `66 passed, 1 deselected`, and MCP is `33 passed, 1 deselected`. Six native inline/O_TMPFILE cases, including nested file/symlink replacement-before-real-register and standalone commit pre/post effect, report `6 skipped, 91 deselected` on Windows and await the manager's native ext4/Python 3.12 run.
- Round 5 ABA hardening: manager native-ext4 evidence proved that a removed publisher inode's `(st_dev, st_ino)` tuple could be reused by a replacement before rollback, allowing the prior snapshot-only journal to delete the replacement. Each journal entry now owns a live descriptor for the exact regular no-follow leaf for the entire outer execution transaction. POSIX opens through anchored `O_DIRECTORY|O_NOFOLLOW` parent dir-fds and compares held `fstat` with lexical no-follow `stat`; Windows uses an `OPEN_REPARSE_POINT`, share-delete handle and the same held/name identity comparison. Because the inode remains pinned, its identity cannot be reassigned. Rollback unlinks only a lexical leaf still matching the held descriptor, replacements remain untouched, and normal/durable commit, rollback, `Storage.close`, registration failure, nested lifecycle, and commit/cleanup `BaseException` paths release each descriptor exactly once without masking an existing primary exception.
- Round 5 ABA TDD/evidence: the portable descriptor-lifecycle RED was exactly `5 failed, 1 skipped, 31 deselected`; all five failures showed that no leaf descriptor was opened or retained. A separate close-cleanup RED was exactly `1 failed` because `Storage.close` masked the rollback primary. GREEN is `7 passed, 2 skipped, 31 deselected`, with the two native POSIX inode-reuse/replace-between-open cases skipped on this Windows host. Storage/document/tree/prepared coverage is `99 passed, 4 skipped`; access/unified is `62 passed`; supported API is `33 passed, 26 deselected`, MCP is `33 passed, 1 deselected`, and CLI is `66 passed, 1 deselected`. The explicit static bypass test passes and the direct-reader search returns no match. All `36` touched/new Python paths pass Ruff format and lint; compileall and `git diff --check` pass. The unsupported full Windows acquisition-lineage run was `68 passed, 9 skipped, 20 failed` solely in its known POSIX-directory/procfs/symlink assumptions; no network/live command ran. Native ext4/Python 3.12 must run the two new ABA cases and the full gate.
- Round 5 ABA native-test model correction: the manager's first native run found exactly `2 failed` in the new tests, not production. One pre-registration replacement test had released every reference to publisher inode A, permitting legitimate ext4 tuple reuse that formal publishers prevent by retaining the shared source/temp/O_TMP handle. It now keeps A alive through a publisher source hardlink and asserts B has a distinct identity before registration. The replacement-hook test swapped `os.stat` without swapping its membership in `os.supports_dir_fd`, unintentionally selecting the fallback path and never reaching the intended post-open hook. It now replaces that capability member with the hook, guaranteeing replacement after the leaf descriptor opens and before no-follow name validation. No production code changed. This Windows host reports the corrected selector as `1 passed, 2 skipped`; the manager owns the corrected native rerun.
- Local review round 6 correction: the shared rollback-required governed-read boundary is now exactly `AccessRejectedError`, every `AccessGatewayError`, and bounded `BodyFailure`. `GovernedAcquisitionGateway`, tree bootstrap/run, and deferred job orchestration propagate those original exceptions instead of converting them to `executor_error`; the outer execution transaction rolls back late page/file transport, origin/redirect, policy/budget, and body failures. Ordinary non-governed executor exceptions retain their prior classified/recorded-failure behavior. Robots-policy decisions keep the frozen #49 `access-rejection-error.v1` unchanged. Non-robots gateway/body failures use the separate stable, redacted `governed-read-error.v1` interface payload, with the original typed class and specific gateway/body error code but no invented access-decision reason code; CLI/API/MCP return the same payload and create no job state.
- Round 6 rollback race correction: rollback no longer performs a vulnerable no-follow `stat` followed by lexical `unlink`. Under one process-wide directory/name lock, it atomically renames the lexical leaf into a cryptographically random exclusive `0700` same-parent quarantine on the same filesystem. Native paths pin and no-follow every parent and the quarantine directory. The private candidate is opened no-follow and compared with the still-held journal descriptor. Only a match is unlinked; a mismatch is atomically restored with a no-clobber hardlink (or Windows no-replace rename). A restore collision preserves the current lexical replacement and the quarantined candidate and surfaces `ExecutionArtifactRollbackError`. All candidate/parent descriptors are attempted exactly once, and cleanup failures do not mask an existing primary exception.
- Round 6 TDD/evidence: governed acquisition RED was exactly `4 failed` because transport, redirect, budget, and body exceptions became `executor_error`. The end-to-end/quarantine RED was exactly `7 failed, 40 deselected`: late transport/origin/budget/body did not propagate, CLI returned success instead of a typed error, no quarantine rename occurred, and restore collision was silent. GREEN focused coverage is `13 passed, 40 deselected` for late page/file zero-state, typed payload/parity, deterministic replace-before-quarantine, restore collision/victim preservation, and cross-Storage name serialization; governed boundary/ordinary executor coverage is `5 passed, 38 deselected`. Storage/document/tree/prepared/staged is `155 passed, 4 skipped`; access/unified/crawler/staged-authority/rescue is `112 passed`; supported API is `33 passed, 26 deselected`, MCP is `33 passed, 1 deselected`, and CLI is `66 passed, 1 deselected`. Native `dir_fd` capability sets are updated in race hooks so manager ext4 runs exercise the secure branch rather than the fallback. No network/live canary ran.
- Local review round 7 correction: BrowserAct is now inspection-only at both exposed execution layers. `BrowserActExecutor.execute` returns `browseract_target_execution_disabled` locally instead of entering the generic subprocess executor, and the stdio wrapper returns the same failure without constructing BrowserAct target arguments. The obsolete stealth/browser-open/session/read-action target helpers are removed; only bounded version, runtime, and help inspection probes remain. The supported-path static inventory now includes both BrowserAct modules and rejects target navigation argument construction. Canonical section discovery now rethrows the shared `ROLLBACK_REQUIRED_READ_ERRORS`, including `BodyFailure`, before `discover_sections` can create output parents, YAML, or reports.
- Round 7 TDD/evidence: the focused RED was exactly `5 failed`: inherited BrowserAct execution reached the wrapper-subprocess path, both stealth and interactive wrapper recipes reached the BrowserAct runner, a robots-404 then 4-MiB-plus-one body failure was swallowed into a zero-page inventory and artifacts, and the static audit found browser-open target arguments. The identical selector is GREEN at `5 passed`. Section discovery/review authority/unified coverage is `30 passed`; the broader access/crawler/document/acquisition/staged/tree/prepared/discovery matrix is `265 passed, 4 skipped`; supported API is `33 passed, 26 deselected`, MCP is `33 passed, 1 deselected`, and CLI is `66 passed, 1 deselected`. BrowserAct supported Windows coverage is `8 passed, 6 deselected`; the unfiltered local attempt is `8 passed, 6 failed`, with all six failures confined to preexisting POSIX/shebang/process-group inspection fixtures unsupported by this Windows/Python 3.11 host. All `39` touched/new Python paths pass Ruff 0.15.12 lint and format checks; compileall, CLI root help, static production search, and `git diff --check` pass. No network/live canary ran.
- Final tested-unreviewed round 8 correction: execution rollback no longer infers directory ownership by walking upward from a removed file. Files retain the existing pinned-inode quarantine rollback. Directories have a separate explicit journal containing only no-follow directories actually created during the active outer execution, with a live descriptor/handle and anchored exact `(device, inode)` verification. Shared tracked-view and canonical-blob publication create parents through that journal; nested inline acquisition transfers only the already-pinned `acquisition_artifacts` and attempt directories it created. Rollback removes files first, then considers journaled directories deepest-first. Preexisting empty parents/ancestors, replacements, symlinks and their victims, and nonempty collisions remain untouched. Nested commit retains ownership until the outer lifecycle; normal/durable commit, rollback, and close release every journal descriptor exactly once, and commit/rollback cleanup still preserves an existing primary exception.
- Round 8 TDD/evidence: the corrected focused RED was exactly `4 failed, 2 passed, 2 skipped`: low-level rollback, tracked view, and governed canonical blob removed preexisting empty directories, and no explicit directory-journal API existed; the new inline pair was skipped because this host has no native `O_TMPFILE`. The same base selector is GREEN at `6 passed, 2 skipped`; expanded provenance/replacement/nested lifecycle/commit-effect coverage is `12 passed, 3 skipped` (only native inline plus unavailable Windows directory-symlink privilege). Storage/document/tree/prepared/staged coverage is `195 passed, 5 skipped`; access/unified/crawler/rescue/review authority is `93 passed`; supported API is `33 passed, 26 deselected`, MCP is `33 passed, 1 deselected`, and CLI is `66 passed, 1 deselected`; the direct governed JSON smoke is `2 passed`. The unsupported full Windows acquisition-lineage attempt is `68 passed, 11 skipped, 20 failed`; the same 20 preexisting failures require POSIX directory fds, `/proc/self/fd`, native unnamed temporary files, or symlink privilege, while the two added skips are the new native inline provenance parameters. The explicit static bypass test passes and the production search has no match. All `39` touched/new Python paths pass Ruff 0.15.12 format and lint; compileall, root CLI help, and `git diff --check` pass. No network/live canary, reviewer round 9, or lifecycle/state action ran; the manager owns the native Python 3.12 full gate and final tested-unreviewed closure.
- Same round 8 native-gate amendment: the manager's native gate reproduced a directory-name race after the exact journal identity check: an interposed rename of owned directory A followed by creation of unrelated empty B at the lexical name caused the old final `rmdir` to delete B. Directory rollback now performs an atomic no-clobber rename from the lexical name to a cryptographically random same-parent transaction-private name while the parent and A remain pinned. It opens and no-follow verifies that quarantined directory against A's held descriptor and removes only an exact empty match. A mismatch is atomically restored with `RENAME_NOREPLACE` on native POSIX or no-replace `rename` on Windows; a restore or initial quarantine collision retains every directory and surfaces `ExecutionArtifactRollbackError`. The shared process lock serializes the name transition. An effect-then-`KeyboardInterrupt` at the quarantine rename is detected through the pinned identity, restores A when the lexical name is free, and rethrows the identical primary exception; cleanup/close errors cannot replace it. A preexisting quarantine name is never reused or removed. All directory journal descriptors remain live until every deepest-first cleanup is considered, then close exactly once.
- Round 8 amendment TDD/evidence: the first secure/fallback-capable selector was RED at exactly `3 failed, 2 skipped` on Windows: replacement B was deleted, restore collision was silent, and the preexisting quarantine namespace was ignored; the secure dir-fd parameters were unavailable locally. A separate effect-after-rename regression was RED at exactly `1 failed, 1 skipped`, preserving the `KeyboardInterrupt` but stranding A under the quarantine name. The post-format focused matrix is GREEN at `8 passed, 3 skipped`; the secure race/collision/effect cases remain native-capable. Storage/document/tree/prepared/staged coverage is `199 passed, 8 skipped`; access/unified/crawler/rescue/review authority remains `93 passed`. Supported API is `33 passed, 26 deselected`, MCP is `33 passed, 1 deselected`, CLI is `66 passed, 1 deselected`, and governed CLI JSON smoke is `4 passed`. The explicit static bypass inventory test is `1 passed`. The unsupported Windows acquisition-lineage run remains exactly `68 passed, 11 skipped, 20 failed`, with all 20 failures in the previously recorded POSIX directory-fd/procfs/O_TMPFILE/symlink class. All `39` touched/new Python paths pass Ruff 0.15.12 format and lint. No network/live canary, reviewer round 9, manager-state write, or lifecycle action ran.
- Remaining work: manager owns the resealed native Python 3.12 full-suite rerun, final tested-unreviewed/local-review-closed recording, and every lifecycle action; no reviewer round 9 is permitted.
