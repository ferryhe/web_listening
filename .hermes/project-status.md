# Project Status

## Issue #69 governed browser-rendered reader (2026-09-05)

- Scope/authority: implementation is complete but uncommitted on `codex/issue-69-governed-browser-rendered-reader` at assigned baseline `480586ca220c276a6c7d813571a23b5d62833950`. No staging, commit, push, PR/Issue mutation, public canary, production browser/cache/state change, scheduled-task change, sibling repository, or downstream repository was used.
- Delivered path: a `browser_rendered` executor can be created only by the staged governed compiler. Its immutable adapter/executor seal the exact `acquisition-execution-plan.v1` object, exact browser step/config/limits, exact read-gateway instance/type/callable, plan fingerprint, and injected session factory. An unprepared adapter, config/fetch-mode/probe/direct wrapper, detached step, replaced adapter/executor, changed gateway call path, or mismatched production gateway fingerprint fails before browser launch or target send. `BrowserCrawler`, BrowserAct, CloakBrowser, and legacy stdio target reads remain disabled.
- Network/content behavior: the existing `GovernedReadGateway` performs the main request and every redirect under its exact-origin, robots, SSRF/pinned-transport, request-budget, and bounded-body rules. Playwright 1.62.0 opens a fresh non-persistent offline context, fulfills only the admitted final navigation from those bytes, aborts all other HTTP requests, closes routed WebSockets, and applies the compiled step's timeout and rendered-byte ceiling. Short real announcements are accepted without an adapter-level word minimum; the caller's existing quality policy remains authoritative.
- Result/failures/cleanup: success returns the existing `FetchResult` plus strict `capture-result.v1` evidence containing requested/final URLs, status, content type, rendered HTML/text/Markdown/fit Markdown, recomputable SHA-256, adapter/runtime identity, and timestamps. Stable browser failures cover missing runtime/binary, launch, timeout, page closed, empty DOM, blocked/interstitial, rendered-content limit, cancellation, and close failure without raw exception text or host paths. Page, context, browser, Playwright driver, and the invocation-owned temporary directory are attempted independently; ordinary close faults do not stop later cleanup, while cancellation, `KeyboardInterrupt`, and `SystemExit` keep primary precedence.
- Fixtures/docs/package: seven canonical idempotent fixtures cover success, redirect, empty content, blocked content, timeout, missing runtime, and cancellation. The acquisition-tools fixture/catalog now names `playwright==1.62.0`; the optional dependency is pinned to that qualified version. README documents the 3.2 capability matrix, explicit operator enablement, authority boundary, no-browser-network design, behavioral rollback, cache ownership warning, and additive SemVer advance to `3.2.0`.
- TDD/validation: the initial focused test was RED at collection because `BrowserAcquisitionExecutor` did not exist; after implementation and the final BaseException-cleanup priority self-review fix, a manager-run one-time Python 3.12.14 environment with exact `playwright==1.62.0` and the qualified Chromium cache was GREEN at `35 passed in 1.50s`; the then-current real `test_qualified_playwright_renders_governed_offline_fixture` alone was `1 passed in 1.01s`, covering the MockTransport redirect, inline-JavaScript DOM, blocked external subresource, normalized results, and recomputable digest without real target network. Catalog CLI help exits 0 and two JSON runs are byte-identical (`acquisition-tools.v1`, seven tools, exact Playwright pin). Python compileall, touched new-file Black, `git diff --check`, the committed Python 3.12.14 qualification validator, and isolated wheel/sdist build for 3.2.0 pass. Build artifacts were removed after their exact workspace paths were verified.
- Review 1 / R1-F1: the accepted AC-1/AC-3 blocker showed that the adapter sealed browser config but the renderer ignored the canonical profile's legitimate `wait_until: networkidle`. New tests were RED at `9 failed, 29 passed, 1 skipped`. Preparation now accepts only the stable `domcontentloaded`, `load`, and `networkidle` values, rejects every unknown key or value with `browser_config_unsupported` before any session or gateway action, seals the normalized value, and passes only that value through the adapter/session into `page.goto`. The fake-session and fake-Playwright tests prove both binding boundaries. The real offline fixture now uses `networkidle` with a delayed DOM update while retaining MockTransport main/redirect reads and an aborted external subresource. Default-host focused coverage was `38 passed, 1 skipped`; browser/staged/profile/qualification was `181 passed, 1 skipped`; the expanded browser/staged/qualification/capture/unified-read/access/fallback/crawler/catalog regression was `286 passed, 1 skipped`. The skip was solely the absent local Playwright 1.62.0 runtime; the current exact manager-runtime result after all four fixes is recorded below.
- Review 2 / R2-F1: the accepted AC-6 blocker showed that taking the minimum timeout across a valid mixed plan silently shortened a 30-second `web_http` step to a browser step's 5-second timeout. New mixed-plan tests were RED at `2 failed, 39 passed, 1 skipped`. The same governed gateway and request/robots budget are retained: its `SafePinnedTransport` now uses the largest declared gateway-backed timeout as an immutable ceiling, while each HTTP or browser main-document read passes its own exact sealed timeout through `GovernedReadGateway` and `AccessGateway`. The per-request value is local to the call and may only narrow the shared transport, so it neither mutates the runtime seal nor enlarges browser authority; redirects reuse that same exact value. Browser navigation independently receives the same browser-step timeout. Response bytes remain shared-max plus the existing exact per-executor read ceiling. Tests prove mixed HTTP 30/browser 5 and equal-timeout plans compile and execute with 8192/4096 byte limits, `web_http`-only retains its pair, a 5-second SafePinned request uses 5 seconds, and an attempted timeout enlargement fails before DNS. At that review point, focused browser evidence was `41 passed, 1 skipped`, AccessGateway was `49 passed`, the core browser/access/site-diagnostic/staged/unified/authority group was `439 passed, 1 skipped`, and expanded related regression was `291 passed, 1 skipped`. Touched-line Black, compileall, and `git diff --check` passed; Ruff was unavailable only in the default interpreter (`No module named ruff`).
- Review 3 / R3-F1: the accepted AC-2/AC-4 blocker showed that the per-step timeout reached only main-document transport and browser navigation, while an uncached initial or redirect-origin robots fetch still used the shared transport ceiling. The exact offline RED was `3 failed, 91 passed, 1 skipped`. `AccessGateway.request_with_context` now passes the same optional local timeout through `_policy_for` and `_fetch_policy` to the robots transport for every origin in the redirect loop; cache hits still make no request, and the caller that owns a single-flight miss alone supplies the timeout for that fetch. No gateway, budget, robots cache, or shared mutable timeout state was added. Tests prove a browser 5-second and HTTP 30-second two-origin chain each apply their value to initial robots, initial main, redirect-origin robots, and redirect main; concurrent 30/5 callers share one policy fetch without value leakage; `None` retains the shared 30-second default; and an attempted enlargement still fails before DNS. Current browser/access GREEN is `93 passed, 1 skipped`; site-diagnostic/staged/unified coverage is `368 passed`; Agentic/prepared-scope regression is `228 passed, 8 Windows capability skips`. Ruff 0.15.12, touched-line Black, Python 3.14 compileall, and `git diff --check` pass. The sole browser skip is the default host's absent Playwright 1.62.0; it is superseded by the manager's exact supported-runtime GREEN below.
- Review 4 / R4-F1: the accepted AC-4 blocker showed that the sealed browser-step timeout bounded governed transport and `page.goto` but left `chromium.launch` at Playwright's longer default. The lifecycle fake was RED at `1 failed, 44 deselected`. `_PlaywrightSession.render` now passes the already normalized `timeout_milliseconds` unchanged to both `chromium.launch` and `page.goto`; no new config or timing abstraction was added. The fake asserts the exact 5000-millisecond launch and navigation values while retaining the existing offline routing and full cleanup checks. Exact GREEN is `1 passed, 44 deselected`; browser/access coverage is `93 passed, 1 skipped` in the default environment. After R4, the manager's isolated Windows environment with Python 3.12.14, exact `playwright==1.62.0`, and the qualified Chromium cache ran the complete browser file GREEN at `45 passed`; native Ruff and `git diff --check` also passed.
- Host/remaining gate: the current supported-runtime browser implementation, including the `networkidle` delayed-DOM fixture and all Review 1-4 fixes, is GREEN in the manager's exact Python 3.12.14 / Playwright 1.62.0 environment. The full Windows/Python 3.12.14 suite completed but is not a pass: `465 failed, 2387 passed, 93 skipped`. Failures retain the established incompatible-host shapes, including Windows/POSIX filesystem and process assumptions and CRLF-sensitive expectations; they are not reader-test failures. Required Linux/Python 3.12 CI remains the full-suite merge gate once the PR exists.

## Issue #58 CloakBrowser 0.5.8 qualification (2026-09-05)

- Scope/authority: work began on branch `codex/issue-58-cloakbrowser-0-5-8-qualification` at assigned baseline `4cbbb0dde6bfaa9b707b7ee4d343c5799860d37c`; the single remote-feedback snapshot is PR #72 HEAD `81055828d6941f45697dc9d8826738ffad00aa7f`. This is isolated qualification evidence only: no production dependency bound/cache, fallback, HTTP/Playwright/CloakBrowser reader, CLI/API/MCP contract, scheduled task, public target, or sibling repository was changed. The implementation worker did not stage, commit, push, or mutate GitHub state. CloakBrowser target capture and direct `BrowserCrawler` target reads remain disabled; #68 is not implemented.
- Result: `tools/qualify_cloakbrowser_0_5_8.py` generates and validates a strict canonical `cloakbrowser-0.5.8-qualification.v1` result. The Windows AMD64/Python `3.12.14` fixture records `cloakbrowser==0.5.8`, wheel `cloakbrowser-0.5.8-py3-none-any.whl` SHA-256 `408e360962298757ef5cce4b0dcda91cee0da8387c9e2b92aab33504eaa4dce6`, and all thirteen exact resolved dependencies. The fixture is self-digesting, closed at every object, byte-idempotent, and supports exactly one conclusion: `defer` (fixture SHA-256 `a5b8256b3f797395048f0fd107f60c73b489983dbc9bbcae95ea6c4857ccdd72`).
- Channel/license evidence: date-stamped 2026-09-05 resolver observations are kept distinct from fixed pins. Stable is fixed at `151.0.7922.108.3` with signed Windows archive SHA-256 `5e03b7abab14d44f2f55368a888378ddca9eefadb08c08b6c28610aec580ab3a` and expected `chromium-151.0.7922.108.3-pro/chrome.exe`; Preview is fixed at `151.0.7922.108.6`, archive SHA-256 `5bd3951f470fff79998a437a139e3e8d68478bc2663402aca1666faf4eadac37`, expected `chromium-151.0.7922.108.6-pro/chrome.exe`. The embedded signed manifests verify offline. No task-scoped paid license/keyed executable was available, so executable digests and real channel lifecycle evidence are honestly absent. Missing and synthetic-invalid keys use unlicensed paths; an instrumented free-plan call proves it drops the exact requested version, so none can qualify keyed Stable/Preview.
- Deterministic boundaries: isolated probes cover missing/corrupt/directory binary, no canonical first-download, launch failure, challenge classification, ordinary close failure with continued teardown, preserved cancellation/`KeyboardInterrupt`, scoped process detection, and task-owned temporary cleanup. The required Stable/Preview loopback lifecycle, navigation timeout, and live cancellation remain `license_unavailable`, not invented successes. Minimum future parameters are only headless, locale, timezone, and humanize; proxy/geo/frame/extension/CAPTCHA/login/session remain boundary-only.
- Validation: four real isolated Python 3.12.14 generations are byte-identical to the fixture; the actual 0.5.8 wrapper routing, signed-manifest, launch-failure, and cleanup probes passed there. Final focused fixture/schema/validator/teardown/disabled-entry/documented-rollback/platform-detection coverage is `26 passed, 1 skipped`; the skip is only the optional actual-wrapper routing probe in the clean project interpreter where CloakBrowser is intentionally absent. A bounded synthetic child-shell proof confirms rollback restores prior-present state and removes only originally absent state without printing the value. Remote feedback correctly identified that the runtime platform tag reused its expected constant; it is now independently derived from detected OS/architecture, with regression coverage proving constant drift cannot satisfy the environment check. Browser/acquisition/fallback/crawler/access/Playwright/package coverage is `178 passed, 1 skipped`. API/OpenAPI, all ten MCP registrations/tools, and contract fixtures are `76 passed, 1 platform skip`; CLI help/list-acquisition-tools tests are `2 passed, 72 deselected`, and direct clean-wheel CLI help, seven-entry machine JSON, canonical validator, and OpenAPI checks pass. Python 3.12.14 wheel build and a fresh wheel[dev] install pass. Final full offline suite is not treated as pass (`464 failed, 2351 passed, 93 skipped`), but an identical clean `origin/main` run is `464 failed, 2327 passed, 92 skipped` and the 464 failing node IDs match exactly with zero differences; that run adds 24 passes and one optional-runtime skip. The failures are the existing Windows-incompatible `os.O_NOFOLLOW`/descriptor/filesystem assumptions and subprocess cleanup behavior, so unrelated diagnosis stopped after the required comparison. Touched-file Ruff 0.16.4 check/format, fixture validation, and `git diff --check` pass.

## Issue #57 Playwright 1.62.0 qualification (2026-09-04)

- Scope/authority: branch `codex/issue-57-playwright-1-62-qualification` remains at assigned baseline `928f16e13ae9b2b48d6de1158da7b7112cd99468`; no commit, staging, push, PR/Issue mutation, production-cache mutation, public live canary, or sibling-repository change was performed. This is qualification only: `BrowserAcquisitionAdapter.capture` and direct `BrowserCrawler` target reads remain disabled; production reader adoption remains #69.
- Delivered: `tools/qualify_playwright_1_62.py` creates a canonical, self-validating `playwright-1.62.0-qualification.v1` result and binds `--browser-cache` directly to `PLAYWRIGHT_BROWSERS_PATH`, so its local lifecycle cannot fall back to the production cache. The committed Windows fixture records exact Playwright `1.62.0`, wheel `playwright-1.62.0-py3-none-win_amd64.whl` SHA-256 `92c0d98ed04eb35af557b709875edba415b1f548bdb22ddb5bb3e1e6c835c2f1`, Chromium revision `1234` / build `151.0.7922.34`, Chrome for Testing `chromium-1234/chrome-win64/chrome.exe` SHA-256 `409805a16d6416087e6b2f778df1cf8f7bbb267d6b99f6b5bb0a618eace234f2`, and the actual launched Headless Shell `chromium_headless_shell-1234/chrome-headless-shell-win64/chrome-headless-shell.exe` SHA-256 `ce4635cd0e5dc0e21494542a701f347e91c1f1d821970578d97ed8df4ced50ef`; dependencies are `greenlet 3.5.5`, `pyee 13.0.1`, and `typing-extensions 4.16.0`, on Windows AMD64 / Python `3.12.14`.
- Lifecycle/cleanup: an isolated browser cache and loopback-only HTTP fixture passed launch of the explicitly selected Headless Shell, context/page, redirect, JavaScript-rendered content, status, timeout, cancellation, missing/corrupt/directory launch failure, close-failure outcome, close, and no remaining Chromium process under the isolated cache. Before the real Playwright driver starts, the lifecycle binds `TEMP`, `TMP`, and `TMPDIR` to one dedicated temporary root; teardown removes and verifies that only controlled root, recorded canonically as `browser_runtime_temp_state_removed: true`. The result is `adopt` for this exact Windows AMD64/Python 3.12.14 package/browser scope; reader adoption remains separately authorized as #69 and needs the documented Python cancellation/API/governed-byte prerequisites.
- TDD/validation: initial new-test import was RED (`ModuleNotFoundError`); fixture tampering was RED until the validator independently pinned every exact identity; the fixture was RED when it lacked the Headless Shell identity; and the direct isolated-cache replay was RED when Playwright resolved the production cache, then GREEN after the runner bound its selected cache. Two Python `3.12.14` real runs are byte-identical to the committed fixture. Final focused adapter/preflight/timeout/redirect/authorization/cleanup/MCP/fixture coverage is `114 passed`; touched-file Ruff check/format and `git diff --check` pass. A fresh UV `0.12.10` temporary CPython `3.12.14` clean wheel[dev] install passed wheel build/metadata, CLI help, acquisition-tools JSON, fixture validation, and OpenAPI checks.
- Current implementation: the real lifecycle and its close-failure probe share one bounded teardown which independently attempts page, context, browser, and Playwright stop after every expected close error; injected failure tests prove later teardown still runs and returns stable `close_failure`. The result validator is closed at every JSON object, recursively type-safe, and total over malformed JSON-like input; extra, missing, and wrong nested values return sorted stable paths without an exception. The CLI maps only expected qualification input/preflight failures—result-read `OSError`, malformed-result `JSONDecodeError` or `UnicodeDecodeError`, and explicit `RuntimeError` from qualification—to deterministic argparse exit 2 errors before any output write; cancellation, interrupts, and unexpected errors are not caught. Generation validates its complete result before stdout or `--write`, so altered wheel/dependency/platform identities, undeclared fields, or malformed checks exit without an artifact. README repeat pins `greenlet==3.5.5`, `pyee==13.0.1`, and `typing-extensions==4.16.0`, saves a caller's prior `PLAYWRIGHT_BROWSERS_PATH`, and restores it after deleting the isolated cache. The documented validator names `py -3.12`; this host's `py` launcher does not register the temporary interpreter, but the exact validator arguments passed under its temporary Python 3.12.14 executable.
- Current validation: schema, teardown, and CLI failure-path tests are GREEN (`41 passed`), including extra root/nested fields, malformed check types, self-validation no-write paths, controlled temporary-state deletion after every injected close failure, and missing/unreadable/malformed/invalid UTF-8 validation inputs plus qualification preflight errors with no traceback or output file. Two real Python 3.12.14 lifecycle runs are byte-identical to the updated fixture with `browser_process_ids: []` and `browser_runtime_temp_state_removed: true`; a separate clean pinned Python 3.12.14 environment downloaded its matching Chromium artifacts into a new temporary cache and reproduced the same fixture with no cache-scoped Chromium process. Qualification/MCP/adapter/preflight coverage is `162 passed`; the related Agentic hardening check is `169 passed`; touched-file Ruff check/format and fixture validation pass. The matched-baseline full-suite investigation was not rerun; an expanded focused command has three Windows CLI failures outside this Issue's touched paths.
- Full-suite evidence: Windows/Python 3.12 full run is not treated as pass (`464 failed, 2296 passed, 92 skipped`). A Docker Linux/Python 3.12 `--network none`, `TERM=dumb`, clean-wheel run from LF-normalized `git archive HEAD` plus this Issue diff was `116 failed, 2732 passed, 5 skipped`; an exactly matched clean-HEAD control under the same image/command was `116 failed, 2721 passed, 5 skipped`. The identical failures are baseline/container evidence (including pre-existing fixture digest and overlay inode/link assumptions), while this Issue adds 11 passing tests; no unrelated product code was changed. GitHub CI at the assigned SHA remains the required merge gate.

## Issue #65 terminal acquisition and batch result (2026-09-04)

- Scope/authority: branch `agent/issue-65` remains at assigned baseline `9fe9ea53104dd008086dfa0e86c35c50b75f4ce5`; no commit, push, PR/Issue mutation, live network, proxy/CAPTCHA bypass, or sibling-repository change was performed. Governed scope execution plus the Issue #53 Agentic lineage remain the supported authority; legacy `check`, API check, BrowserAct target execution, and direct developer-tool target reads remain disabled.
- Result: one shared HTML terminal classifier now rejects HTTP 403, block/CAPTCHA content, same-origin block redirects, other non-2xx status, and empty extracted content with stable reason codes. Wrapper, Legacy compatibility, and Governed outcomes store those as failed `CaptureResult` records with no successful content. Agentic rejects block/empty pages before `ArtifactStore`, so only genuine successes create immutable artifacts.
- Contract: strict `acquisition-batch-result.v1` emits one disposition/reason per externally requested site/seed; discovered reads and retries remain separate valid-snapshot/failed-evidence counts. Its authoritative terminal status prevents a partial parent from being reported as full success. Existing governed `run-scope --json` now persists and emits this canonical result through `job_delivery.v1`; page/file failures persist a failed job and exit nonzero, while a fully successful site remains completed with zero exit. This is not another database, a new batch/network authority, or the Issue #54 exporter. The fixture round-trips idempotently.
- R1 accepted fixes: compound same-origin `/cdn-cgi/challenge-platform/...` redirects are classified as `blocked_redirect`; raw `script`/`noscript` marker text no longer rejects substantive extracted content, while visible CAPTCHA/block content remains rejected. Reviewer reproductions for external-request cardinality, authoritative partial status, governed CLI/job delivery, redirect detection, and false-positive script markers are covered by focused regressions.
- R2 accepted classifier fixes: the visible phrase `Please verify you are human to continue` is stable failed block evidence across Legacy, wrapper, Governed, and Agentic consumers. Generic `Cloudflare` and `Enable JavaScript` text is no longer decisive on substantive pages, and generic `challenge`/`verify`/`verification`/`block` path segments no longer turn ordinary same-origin topic redirects into block redirects. Exact `/captcha` and `/access-denied` endpoints plus compound `/cdn-cgi/challenge-platform/...` remain rejected. The R2 RED was `9 failed, 17 passed`; the same selection is GREEN at `26 passed`.
- R3 accepted classifier fix: Governed quality-gate markers now use the same shared high-confidence matcher as terminal classification. Existing profiles may still declare the three proven generic defaults (`cloudflare`, `enable javascript`, `forbidden`) for compatibility, but those strings alone are not decisive; configured counts remain the truthful source count and genuinely custom phrases still match. A real default-profile Governed selection was RED at `2 failed, 6 passed` and is GREEN at `8 passed`.
- R4 accepted classifier fix: the exact high-confidence Cloudflare block-page phrase `Sorry, you have been blocked` is rejected even on an unchanged HTTP 200 URL, without restoring generic `cloudflare` matching. Shared-classifier and wrapper regressions were RED at `2 failed, 4 passed` and are GREEN at `6 passed`.
- R5 accepted API fix: the existing `/api/v1/jobs/{job_id}/payload` response now declares and strictly validates optional `acquisition_result` as `AcquisitionBatchResult`, so persisted run-scope counts and dispositions are no longer filtered from downstream JSON. A persisted mixed/failed endpoint regression was RED with missing `acquisition_result` and is GREEN at `1 passed`.
- R6 accepted initial-admission fix: a non-accepted governed seed now crosses a dedicated plan/outcome boundary into `run-scope`, which persists one failed requested-site disposition with a stable allowlisted capture reason, zero valid snapshots, truthful failed evidence, and exit 1. Arbitrary staged/runtime errors and rollback-required authority failures retain their existing generic/governed command errors and are not persisted as site failures; bootstrap behavior is unchanged. The exact R6 selection was RED at `1 failed, 4 passed` and is GREEN at `5 passed`; focused CLI/API/staged/Agentic/job coverage is `288 passed`.
- Remote-feedback fix: a legal nonterminal Agentic seed remains unresolved but now reports its truthful stable fallback (`task.running` or `task.queued`) instead of `task.cancelled`; a real cancellation and an explicit stable failure code remain unchanged. The exact selection was RED at `2 failed, 2 passed` and is GREEN at `4 passed`; expanded focused coverage is `292 passed`.
- TDD/verification: initial selected RED was `14 failed` (11 reproduced false-success/classification behavior, two lacked the batch projection, and one test authority fixture was corrected before implementation); exact selected GREEN is `14 passed`. Focused acquisition/Agentic/hardening is `238 passed`; Agentic hardening alone is `169 passed`. `py -3.14 -m build`, wheel metadata/content checks, source CLI help/tool/contract/BrowserAct smoke, legacy-check disablement, compileall, Ruff 0.15.12 check, new-file Ruff format, and `git diff --check` pass.
- Host limit: the final required `py -3.14 -m pytest -q` completed with `2269 passed, 92 skipped, 481 failed` in 117.01s (four more passing remote-feedback regressions and no added failures versus the committed Issue head; `+50` passed and `-1` failure versus the manager's clean baseline). The project declares Python `>=3.12,<3.13`; this Windows host has 3.14 and 3.11 but no 3.12. The remaining failures match the established host baseline in unchanged POSIX-specific areas (`os.O_DIRECTORY`, directory descriptors/symlink privileges, subprocess cleanup and resulting CLI/test cascades). The separate Linux/Python 3.12 required check currently has 18 deterministic pre-existing Typer dependency-drift parser failures; that merge gate is not an Issue #65 finding and was not changed here. The Issue #65 focused paths are green.
- Required-CI compatibility follow-up: after explicit scope expansion, Typer's vendored `UsageError` and optional `Exit` imports are probed independently. This preserves the external-Click fallback for Typer 0.12+ while correctly catching Typer 0.27 parser errors when that release vendors `UsageError` but not `Exit`. The exact CI-equivalent selection moved from `17 failed, 1 skipped` to `17 passed, 1 skipped` locally (the skipped unreadable-file case is POSIX-only and was the eighteenth Linux failure). The final Windows/Python 3.14 suite is `2286 passed, 92 skipped, 464 failed`, exactly converting those seventeen local parser failures with no new failure; the remaining failures retain the established host-platform shapes.

- Date: 2026-08-24 America/New_York.
- Project/worktree: `web_listening` at `C:\Project\web_listening\.worktrees\issue-53`.
- Branch/base: `codex/issue-53-agentic-orchestration` from exact clean baseline `8cb414b16a21f3fb884e90e4150509dd432178dc`.
- Boundary: sole delegated implementation worker for GitHub Issue #53 under parent Epic #46. Sibling repositories/worktrees, other Issues, final acquisition-manifest production (#54), classification/knowledge-base/chunk/index/ready work, live targets/network, subagents, commits, staging, pushes, PR/Issue mutation, merge, cleanup, and manager review state remain off-limits.
- Authority preserved: every target-content read enters the Issue #51 `GovernedReadGateway`; crawler and authorized-search adapters only propose inert typed candidates. Every successful original enters the Issue #52 `ArtifactStore.store_observation` path. No direct or auto-follow content transport, browser target read, mutable-original write, default authority, credential/log artifact, or new live-site behavior was added.
- Delivered contract: additive strict `agentic-site-rules.v1` YAML binds a stable semantic digest over rule ID/version, site key, canonical credential-free seed URLs, exact allowed origins, URL/path allow globs, optional/required queries, allowed MIME types, and exact depth/request/decoded-byte/file/concurrency/retry budgets. YAML is UTF-8, size-bounded, duplicate-key-free, alias-free, closed-shape, exact-integer/boolean validated, and accompanied by the committed offline sample `docs/testing/fixtures/agentic-site-rules-v1.sample.yaml`.
- Delivered orchestration: additive `agentic-orchestration.v1` now requires a `PreparedAgenticAuthority` produced by recompiling trusted scope/profile/resolved-Site-Skill/executor-registry inputs and exact-matching the complete canonical plan. The non-replaceable capability seals the full Site Skill and plan payload to one exact concrete `AccessGateway` and `ArtifactStore`; forged dataclass plans, subclasses, shaped readers/stores, changed gateway identity, and authority enlargement fail closed. The explicit `httpx.MockTransport` facade remains the offline-only seam.
- Durable state: additive versioned `agentic_runs`, `agentic_tasks`, and `agentic_observations` tables retain stable digest-derived IDs and complete discovery/read/replay lineage. Additive migration and guards permit observation insertion only for a running run/task. Atomic finalization plus strict terminal load verify contiguous attempts, coherent statuses, final completed observation/task evidence, and exact existing immutable-artifact run/request/final/access/adapter lineage. Persisted lease, atomic budgets, sealed required children, immutable terminals, and replay remain fail closed and idempotent.
- Failure semantics: robots rejection/error, gateway/body/metadata/redirect/policy failure, unsupported content, final scope rejection, artifact-store rejection, search/crawler failure, budget exhaustion, cancellation, and bounded retry produce stable state without persisting exception messages, response bodies, credential values, or host paths. Governed body/metadata/redirect/post-reservation-policy failures retain access decision, current/final URL, status when present, and redirect context. Candidate validation and sorting stay within the bounded crawler guard, so malformed output yields stable `crawler.discovery_failed` partial state. Optional failure produces `partial` plus `optional_child_failed`; cancellation remains `cancelled`; no-success required rejection/failure remains `rejected`/`failed`.
- Integration surface: `GovernedReadGateway.read` and its offline mock facade accept an optional positive `max_body_bytes` that can only narrow the configured ceiling and return truthful wire/decoded-byte, wire/content-encoding, bounded safe response-filename, and contextual failure evidence. Artifact persistence, usage accounting, agentic observation, and successful task terminal state share one storage transaction; cancellation, `BaseException`, store failure, and SQLite failure compensate to a stable terminal run without orphan files or duplicate evidence. Existing non-agentic callers remain source-compatible. The Agentic module is additive and does not alter the canonical staged CLI/API/MCP authority or create a second planning/execution interface.
- SemVer/docs: project source expectation is `3.1.0`, classified Minor because Issue #53 adds an optional orchestration capability and additive tables without changing the 2.0 access authority or 3.0 immutable identities/storage. Root README documents the new contract, task semantics, limits, identities, safety, replay, non-goals, fixture, and version decision.
- TDD evidence: Round 1 retained its exact RED/GREEN history. Round 2 added exactly five initially failing tests: forged plan accepted, AccessGateway subclass accepted, terminal ledger accepted missing final evidence, BodyFailure lost its decision context, and malformed crawler output escaped at sort. All five are GREEN; coverage was then extended to metadata, redirect, and post-reservation policy context plus terminal-state observation insertion.
- Focused GREEN: `tests/test_agentic_orchestration.py tests/test_agentic_orchestration_hardening.py tests/test_unified_read_paths.py tests/test_access_gateway.py` is `89 passed`. It covers trusted compilation/sealing, forged/subclass rejection, contextual failure persistence, strict terminal evidence, stable malformed discovery, and all earlier complete/partial/reject/fail/cancel/budget/retry/replay/transaction behavior.
- Broad GREEN: `tests/test_agentic_orchestration.py tests/test_agentic_orchestration_hardening.py tests/test_access_gateway.py tests/test_unified_read_paths.py tests/test_immutable_artifact_store.py tests/test_storage.py` is `261 passed, 39 skipped`; skips are the already-documented Windows/native-POSIX capability cases.
- Expanded evidence/known host limits: adding acquisition-execution-plan and Site Skill protocol suites yielded `604 passed, 39 skipped, 45 failed`; all 44 plan failures stop in the existing Windows/Python 3.11 lack of `os.O_DIRECTORY`, and the one Site Skill fixture-byte digest failure is the existing Windows checkout line-ending mismatch. Focused Issue #53 and touched gateway/storage paths remain green. `tests/test_release_version.py` reaches the new source `3.1.0` assertion but the unmodified host still exposes stale installed `web-listening==1.1.0` metadata.
- Final static/tooling GREEN: touched-file Ruff check, Ruff format check, Python compile, `git diff --check`, and `web-listening --help` all passed. HEAD remains the assigned base `8cb414b16a21f3fb884e90e4150509dd432178dc`; no commit or staging was performed. Manager/reviewer owns the mandatory fresh read-only review gate, native Python 3.12/ext4 checks, clean package-install version verification, and every commit/push/PR/lifecycle action.
- Pre-review static follow-up: resolved all diagnostics reported by manager's Ruff 0.16.4 run across the touched Python files (imports, UTC modernization including direct Python 3.12 `Z` parsing, annotations, exception type, set comprehension, explicit external-boundary exception tuples, and sorted exports) without `noqa` or behavior expansion. The hardening batch also clears `RUF005,RUF043,SIM102,B014,FURB162`; one pre-existing unknown `asyncio_mode` warning remains in local test output.
- Final Ruff 0.16.4 compatibility follow-up: removed two unused test unpack bindings and converted `AccessGatewayResult` to the requested Python 3.12 type-parameter syntax (`class AccessGatewayResult[T]:`) without runtime behavior changes. Local Ruff 0.15.12 default plus the matching preview selectors and format check pass, and the available Python 3.14 interpreter compiles the updated gateway. This Windows host has test dependencies only under Python 3.11, so the post-syntax focused pytest attempt stops during collection at the expected Python 3.11 parse error; the immediately preceding focused run was `89 passed`, and manager owns the required native Python 3.12 rerun.
- Round 3 TDD evidence: one batch added exactly ten initially failing regressions, one for each accepted review item (compiled-scope sealing, offline mock identity, immutable-store configuration, closed provenance, per-target request accounting, durable failure context, candidate-kind semantics, exact parent outcome, v2 trigger migration, and compatible terminal replay). The exact RED was `10 failed`; the exact selected GREEN is `10 passed`.
- Round 3 authority/budget result: the prepared capability now seals the semantic `MonitorScopePlan`, exact seed/homepage/origins/page/file prefixes, exact offline MockClient/MockTransport identities, and exact ArtifactStore/Storage/resolved-root/storage-encoding/MIME configuration. Rules, patterns, seeds, candidates, and final URLs must be a subset of compiled scope. Every redirect target send and retry atomically consumes a persisted run request unit immediately before transport; robots-policy fetches are explicitly outside that target-content counter. Seals are revalidated before each run/read/store boundary, so post-prepare swaps fail before I/O or either database/root receives an orphan.
- Round 3 ledger/result: additive `agentic-ledger.v2` migration adds durable `current_url` and `status_code`, transactionally replaces every module trigger, and strictly validates closed redirect/discovery provenance on add/load/finalize. Parent outcomes use one pure derivation in execution/finalization/load; candidate kind/source rules are closed at the adapter boundary; replay sources must be terminal and rules/Site-Skill/plan/adapter compatible, with task links constrained to the exact parent replay source and same kind/key.
- Round 3 verification: before restoring the required Python 3.12 PEP 695 declaration, focused compatibility was `99 passed` and bounded broad storage/access/agentic coverage was `271 passed, 39 skipped` under Python 3.11 (only the pre-existing unknown `asyncio_mode` warning). After restoration, local Ruff 0.15.12 default, Ruff preview selectors matching the manager's 0.16.4 findings, Ruff format check, Python 3.14 compile, and `git diff --check` pass. Ruff 0.16.4 and dependency-complete Python 3.12 are not installed on this Windows host; manager owns those native reruns. No lifecycle/network/state action was performed.
- Round 4 TDD evidence: one bounded batch added exactly six initially failing regressions covering the accepted gateway seal, HTTP-only executor, page/file scope and budget, closed failure provenance, post-artifact interruption/recovery, and version-gated real-v1 migration requirements. Exact RED was `6 failed, 25 deselected`; exact GREEN is `6 passed, 25 deselected`. Two supplemental assertions then reproduced and fixed internal Mock gateway-state replacement and SQLite masking of an original `KeyboardInterrupt` (`2 failed, 1 passed` to `3 passed`).
- Round 4 authority/scope result: preparation admits only exact `web_http` steps with exact `http_get` capabilities and records that honest executor lineage. It seals the normal gateway configuration and exact transport plus the offline wrapper/client/`MockTransport` and each inner gateway's stable state containers; post-prepare configuration, transport, wrapper, user-agent, and internal-state replacement fail before target I/O. Final governed URLs are classified by MIME and checked against separate compiled page/file prefixes; successful pages/files increment separate durable counters under the plan/rule ceilings, including after redirects.
- Round 4 durability result: failure provenance now enforces requested/last-accepted current endpoints and permits a different final target only for a closed 3xx redirect rejection. Post-artifact cancellation/interruption derives a global terminal parent outcome even after all children are terminal, recovery atomically clears stale active reads, and secondary SQLite interruption bookkeeping cannot mask the original `BaseException`. The explicit ledger version gate rejects unknown/future versions; a physical v1 fixture lacking v2 columns upgrades transactionally by backfilling completed current URL/status and page/file usage from immutable artifact evidence before marking v2 and replacing guards.
- Round 4 verification: exact selected is `6 passed, 25 deselected`; focused agentic/access/unified-read coverage is `105 passed`; bounded broad agentic/access/immutable-storage coverage is `277 passed, 39 skipped`. The only local warning is the pre-existing unknown `asyncio_mode` option under Python 3.11. After restoring the required Python 3.12 PEP 695 declaration, local Ruff 0.15.12 default plus the preview selectors corresponding to the manager's Ruff 0.16.4 findings, Ruff format check, Python 3.14 compile, and `git diff --check` pass. Ruff 0.16.4 and dependency-complete Python 3.12 are unavailable on this Windows host; manager retains those native reruns.
- Round 5 TDD evidence: one bounded batch added exactly five initially failing regressions, one for each accepted review item. Exact RED was `5 failed, 31 deselected`; exact selected GREEN is `5 passed, 31 deselected`. The tests cover exact production/offline call-path seals, closed decisionless failure evidence, adapter call/mid-iteration exceptions without swallowing scheduling failures, full v1 task/evidence rollback, and a read-only version/shape preflight with SQLite authorizer proof of zero schema action.
- Round 5 authority/failure result: production Agentic preparation now accepts only exact `SafePinnedTransport` semantics and seals transport identity/configuration plus callable/class behavior; instance-shadowed requests, changed class methods, and arbitrary normal transports fail before I/O. `MockClientReadGateway` is the sole offline seam and revalidates the exact client, `MockTransport`, handler identity/code/call behavior, wrapper, and full prepared cross-origin inner-gateway set before every read. Initial run-budget, pre-reservation gateway policy/budget, and robots safety/transport failures may carry a null decision only under the closed reason/status/endpoint model; accepted redirect proof IDs survive a later-origin policy failure, and every path releases `active_reads`.
- Round 5 adapter/migration result: only the untrusted search/crawler call plus bounded materialization catches ordinary `Exception`; `BaseException` and repository scheduling failures propagate. A read-only ledger preflight now rejects unknown/future/renamed/malformed markers and missing required columns before any DDL/index/trigger action. Exact v1 then migrates in one transaction, validates every task, attempt, terminal/artifact/replay reference before marking v2, and explicitly rejects running v1 runs after validating their rows/evidence; any failure restores schema, data, indexes, triggers, and marker exactly.
- Round 5 verification: focused agentic/access/unified-read coverage is `110 passed`; bounded broad agentic/access/immutable-storage coverage is `282 passed, 39 skipped`. The only warning is the pre-existing unknown `asyncio_mode` option under the local Python 3.11 dependency set. Local Ruff 0.15.12 default and matching preview selectors plus format pass after the one justified `BLE001` adapter-boundary annotation; final PEP 695 restoration, Python 3.14 compile, and diff check are recorded in the handoff. Manager retains Ruff 0.16.4 and dependency-complete native Python 3.12/full-suite reruns.
- Round 5 full-suite compatibility follow-up: manager's native full run exposed exactly 16 legacy crawler/document/staged/tree failures because their target-only `MockClientReadGateway` handlers began receiving robots requests. The offline facade again returns its historical synthetic `/robots.txt` 404 by default without invoking those handlers. Only sealed Agentic preparation makes a one-way switch to handler-driven robots, and that mode plus both capability identities are included in the wrapper/prepared seal and revalidated before I/O. The representative RED was `16 failed, 97 passed`, plus one exact mode/seal regression RED; GREEN is `113 passed` for all four affected legacy files, `6 passed, 31 deselected` for all Round 5 cases, and `111 passed` for focused agentic/access/unified-read coverage. No production authority, handler identity check, network path, or public surface changed.
- Round 6 TDD evidence: one bounded batch added exactly eight initially failing regressions, one for each accepted review item. Exact RED was `8 failed, 37 deselected`; exact selected GREEN is `8 passed, 37 deselected`. The cases cover per-send redirect scope/reservation, frozen gateway/store/transport call paths, production timeout/chunk limits, monotonic lease fencing and takeover, exact current-schema preflight, preserved transport retryability, finite unknown-transport evidence, and adapter identity drift.
- Round 6 authority/execution result: every target send now checks the current URL against both rules and the compiled page-or-file scope and consumes one persisted request unit immediately before transport, while retaining the final MIME-specific page/file gate. Production seals the exact `SafePinnedTransport` request/address-resolver behavior, timeout, and bounded chunk size; offline seals its exact mock wrapper/client/transport/handler mode. Gateway read/request and immutable store/get call paths are revalidated and invoked through frozen exact callables, so instance shadows or class mutations fail before I/O or a write. Search/crawler adapter object, identity/version, authorization, bound function, and bound receiver snapshots are revalidated after invocation and lazy materialization before persistence or scheduling.
- Round 6 durability/failure result: run leases now carry a persisted monotonic epoch and local owner/epoch fence. Request/budget/task/observation/artifact/finalization/interruption/recovery/release boundaries validate that fence, and critical updates use the exact owner/epoch predicate so an expired owner cannot send, publish, or clear its successor. Current-v2 preflight validates full ordered column shape, defaults/keys, exact tables, indexes, uniqueness, triggers, and object kinds before schema action. Gateway transport retryability is retained for the closed connect/disconnect contract; decisionless reasons are finite and an unknown kind maps only to `gateway.transport.unclassified_transport`.
- Round 6 verification: the exact selected batch is `8 passed, 37 deselected`; hardening reached `45 passed`; focused agentic/access/unified-read coverage is `119 passed`; bounded broad agentic/access/immutable-storage coverage is `291 passed, 39 skipped`. After the final SQL fence-predicate audit, the eight Round 6 cases plus the affected late-observation guard are `9 passed, 36 deselected`. The only local warning is the pre-existing unknown `asyncio_mode` option under Python 3.11. Final local Ruff 0.15.12 default and the preview selectors matching prior manager Ruff 0.16.4 diagnostics pass, as do the Issue #53 source/test format check, Python 3.14 compile with the required Python 3.12 PEP 695 declaration restored, and `git diff --check`. The legacy `site_diagnostic.py` file is not globally Ruff-format-clean at the assigned base, so it was not bulk-reformatted; its additive address-seal block is locally formatted and default Ruff-clean. Manager retains the native Ruff 0.16.4, dependency-complete Python 3.12, and full-suite reruns.
- Round 6 native-static compatibility follow-up: resolved all 18 manager-reported Ruff 0.16.4 diagnostics in `site_diagnostic.py` without changing transport outcomes or the frozen `_addresses` SSRF defense. Imports use `collections.abc`/`UTC`; URL/BOM/unused/lambda-capture fixes are mechanical. Socket cleanup now suppresses only `OSError`; untrusted response cleanup preserves prior ordinary-exception suppression, the primary protocol/body result, and cancellation semantics; the intentional untrusted transport `Exception` boundary is documented. Local Ruff 0.15.12 default plus all corresponding preview selectors pass, relevant site/access/unified/Agentic coverage is `372 passed`, Python 3.14 compile and `git diff --check` pass, and the six Issue #53 formatter-owned files remain format-clean. The assigned-base legacy file still fails a whole-file Ruff format check and was deliberately not bulk-reformatted. `immutable_artifacts.py` has no content diff; aggregate scope remains twelve paths.
- Round 6 legacy retryability compatibility follow-up: preserved explicit `AccessGatewayTransportError.retryable` values while restoring omitted-argument inference from the finite approved transient kinds `connect`, `connect_or_http`, `dns`, `network`, `remote_disconnected`, and `timeout`. Unknown and safety kinds remain non-retryable and reason acceptance is unchanged. The native regression was reproduced RED (`1 failed`) and the complete prepared-scope failure matrix plus the new finite-set/explicit-false regression is GREEN (`6 passed`); Round 6 selected is `9 passed, 37 deselected`, focused prepared/access/unified/Agentic is `179 passed, 8 skipped`, and bounded broad is `351 passed, 47 skipped`. Local Ruff default and matching preview selectors, format, Python 3.14 compile with final PEP 695 syntax, and `git diff --check` pass.
- Round 7 TDD evidence: one surgical batch added exactly three initially failing regressions for atomic offline robots preparation, persisted send-claim lease fencing, and strict v1 read-only schema preflight. Exact RED was `3 failed, 46 deselected`; exact GREEN is `3 passed, 46 deselected`. A failed Mock preparation leaves legacy handler mode, gateway identity, and policy cache unchanged; successful preparation replaces the legacy cache only after validation and makes robots policy handler-driven before any target request. Each target send now owns a persisted epoch-bound claim from budget consumption through transport completion, so an expired fence sends nothing and takeover cannot race a committed reservation; release restores the prior lease for deterministic successor recovery without holding a database transaction across transport.
- Round 7 migration/verification: v1 preflight now permits only the exact known legacy table/column/index/trigger subset before `BEGIN`; alien columns, indexes, objects, and triggers fail with `ledger.version_unsupported` and byte-for-byte logical dump preservation, while a valid migration reopens as exact v2. Hardening is `49 passed`; focused Agentic/access/unified-read coverage is `123 passed`; bounded legacy crawler/document/staged/tree/site/storage compatibility is `571 passed, 39 skipped`. Local Ruff 0.15.12 default and format checks pass, the Round 7 `B023` preview finding is fixed, Python 3.14 compile with final PEP 695 syntax and `git diff --check` pass. The exact twelve-path scope remains unchanged; manager retains native Ruff 0.16.4 and dependency-complete Python 3.12/full-suite reruns.
- Round 8 TDD evidence: one bounded batch added exactly three initially failing regressions for response-lifetime send claims, synchronized Mock preparation, and v1 object ownership plus post-migration validation. Exact RED was `3 failed, 49 deselected`; exact GREEN is `3 passed, 49 deselected`. The persisted epoch-bound claim now remains held through lazy body iteration, redirect cleanup, response close, ordinary failure, cancellation, and `BaseException`, without holding a SQLite transaction across transport. Release is idempotent, cannot clear a successor epoch, and restores either the still-usable original fence or its safe expired state so takeover becomes possible only after cleanup.
- Round 8 synchronization/migration result: the offline Mock facade uses one exact sealed reentrant lock for preview, commit, gateway selection, and the read snapshot; the transition publishes gateway/origin/robots mode state as one reader-visible unit, and swapped locks or transition call routes fail the prepared seal. Ledger preflight retains `sqlite_master.tbl_name` and SQL, binds each known index/trigger to its exact owning table, requires canonical named-index SQL, and checks exact v2 trigger definitions. Genuine v1 migration validates evidence, transactionally rebuilds all three tables into canonical v2 column order, replaces indexes/guards, and reruns the complete v2 preflight before commit; any ownership or final-shape fault rolls back to the exact v1 dump, and clean migration reopens as v2.
- Round 8 verification: hardening is `52 passed`; focused Agentic/access/unified-read coverage is `126 passed`; bounded Agentic/access/immutable-storage coverage is `298 passed, 39 skipped`; the four legacy Mock consumers remain `113 passed`. Local Ruff 0.15.12 default plus `I,UP,PIE,BLE,FURB,B` selectors and format check pass, as do Python 3.14 compile with final PEP 695 syntax and `git diff --check`. The actual scope remains exactly twelve paths; manager retains native Ruff 0.16.4, dependency-complete Python 3.12, and full-suite reruns.
- Round 8 send-claim deadline follow-up: the existing response-lifetime test was tightened to prove that a stream lasting beyond the original one-second lease but within the ten-second configured send timeout leaves the original owner able to complete its next fenced budget mutation. The exact RED was `1 failed, 51 deselected` with `run.lease_lost`; the claim token now retains the already bounded send deadline, so idempotent release restores a future owner fence, excludes a successor until that deadline, and permits takeover after expiry. Exact GREEN is `1 passed, 51 deselected`, all Round 8 cases are `3 passed, 49 deselected`, hardening is `52 passed`, and focused Agentic/access/unified-read coverage is `126 passed`.
- Round 9 TDD evidence: one bounded batch added exactly four test functions for the accepted complete gateway-call-graph seal, whole-response send-claim renewal, primary-preserving release cleanup, and canonical ledger-table semantics. The cleanup case is parameterized over body failure, redirect close, cancellation, and `KeyboardInterrupt`, so exact RED was `7 failed, 52 deselected`; exact GREEN is `7 passed, 52 deselected`.
- Round 9 authority/lease result: Agentic preparation seals the complete `AccessGateway` authorization call graph, module transport dispatcher, exact config/transport, and per-transport identity behavior. The seal is rechecked at request entry and again after target reservation immediately before the frozen dispatcher, so helper/dispatcher/config/transport swaps reject before target I/O. A callable epoch-conditioned send guard renews the persisted claim across DNS, connection/TLS, header exchange, each lazy body read, and close; fence loss stops consumption and closes the response, while successful release restores the latest bounded future deadline. Release cleanup cannot mask any primary `Exception`, cancellation, or `BaseException`; it remains the propagated error only when no primary exists.
- Round 9 ledger/verification: preflight now requires exact canonical normalized table DDL, complete `table_xinfo` including hidden/generated state, no foreign keys, ordinary rowid storage, and non-STRICT options for both current v2 and the closed ordered v1 legacy subsets. Migration rebuilds canonical final v2 tables directly and reruns the full postflight before commit. Hardening is `59 passed`; focused Agentic/access/unified-read is `133 passed`; bounded Agentic/access/immutable-storage is `305 passed, 39 skipped`; Site Diagnostic is `253 passed`; prepared-scope execution is `59 passed, 8 skipped`; legacy Mock crawler/document/staged/tree is `113 passed`. Local Ruff default and corresponding preview selectors, the seven formatter-owned paths, Python 3.14 compile with final PEP 695 syntax, and `git diff --check` pass; native Ruff 0.16.4 and dependency-complete Python 3.12 remain manager-owned.
- Round 9 residual seal follow-up: exact RED was `16 failed, 59 deselected`, proving fourteen mutable access-gateway execution aliases and two deterministic response-time config/transport swaps escaped the prior seal. Each gateway now retains a read-only original-callable dispatch, seals every legacy `_FROZEN_ACCESS_*`, SafePinned, and transport-dispatch alias, and atomically validates plus snapshots config/transport under one short runtime lock before sending; transport return is revalidated and closed before any redirect or secondary policy I/O. Exact residual GREEN is `16 passed, 59 deselected`; all Round 9 cases are `23 passed, 52 deselected`; hardening is `75 passed`; focused Agentic/access/unified-read is `149 passed`. Local Ruff default and matching preview selectors, format, Python 3.14 compile with final PEP 695 syntax, and `git diff --check` pass. No scope, network, lifecycle, Git, or manager-state action was added.
- Round 9 Mock-capability residual: exact RED was `3 failed, 75 deselected` for response-time wrapper-request shadow, client-transport swap, and handler swap. Gateway sends now invoke the exact validated unbound transport request and reject transport instance shadows; the offline wrapper retains exact `MockTransport`/handler capabilities, invokes that captured handler directly without mutable `client.send`, and is revalidated immediately after each transport return. Exact residual GREEN is `3 passed, 75 deselected`; all Round 9 cases are `26 passed, 52 deselected`; focused Agentic/access/unified-read is `152 passed`; legacy crawler/document/staged/tree compatibility remains `113 passed`. Local Ruff default and matching preview selectors, format, Python 3.14 compile with final PEP 695 syntax, and `git diff --check` pass. No real transport was constructed or invoked.
- Round 10 TDD evidence: the bounded batch reproduced exactly `26 failed, 78 deselected`: seven mutable duplicate orchestrator capability attributes, eight robots/access helper aliases, five governed-read helper aliases, two governed reader authority attributes, one response-time helper swap, one noncanonical persisted timestamp, one missing identifier/terminal lease guard, and one malicious same-name v1 trigger. The exact original batch is GREEN at `26 passed, 78 deselected`; two supplemental zero/nonzero-microsecond lease/send direction cases bring the Round 10 selection to `28 passed, 78 deselected`.
- Round 10 authority/helper result: `AgenticOrchestrator` now retains one private prepared capability and exposes only read-only derived gateway/store/Site-Skill/plan/authority/storage properties; its repository is bound to that prepared storage and each execution boundary revalidates the prepared seal. Governed reads retain one synchronized exact gateway/body-limit snapshot and immutable helper dispatch. Prepared access seals now cover cache-key, robots policy construction/evidence, bounded-body, MIME/decode/markup/parser helpers, plus the governed body/context/metadata call graph. Instance, class, or module helper/capability changes fail closed before using a mutable route; unsealed legacy gateway tests retain their existing explicit monkeypatch seam.
- Round 10 ledger result: persisted ledger timestamps are canonical UTC with exactly six microsecond digits, and exact v1/v2 legacy zero-microsecond values normalize inside the existing schema transaction before validation. Same-second zero-to-nonzero and nonzero-to-zero lease acquisition, send claim, renewal, release, and the next fenced mutation compare correctly. Guards now freeze `run_id` and `task_id` in every state and `lease_epoch` after terminal state. Current trigger hashes are canonical, while only the exact known prior-v1 trigger hashes are additionally accepted; a malicious same-name trigger is rejected before DDL with an unchanged logical dump.
- Round 10 verification: hardening is `104 passed`; focused Agentic/access/unified-read is `180 passed`; bounded Agentic/access/immutable-storage is `352 passed, 39 skipped`. Local Ruff 0.15.12 default, the new closure-specific `B023` preview check, Ruff format, Python 3.14 compile, and `git diff --check` pass; final source retains the required Python 3.12 PEP 695 declaration. The local warnings remain the existing unknown `asyncio_mode` option and Starlette/httpx deprecation. Content scope remains exactly twelve paths (`immutable_artifacts.py` still has no content diff); manager retains native Ruff 0.16.4, dependency-complete Python 3.12, and full-suite validation.
- Round 11 TDD evidence: the bounded batch reproduced exactly `13 failed, 106 deselected` across four exact-rules/prepared-predicate mutations, one mid-run rules mutation, seven SafePinned helper/alias mutations, and one forged observation task. The exact batch is GREEN at `13 passed, 106 deselected`.
- Round 11 rules and transport result: each run captures one exact `AgenticSiteRules` semantic JSON/digest and trusted clone, then revalidates the original rules plus frozen rule/prepared predicates at candidate, target-send, final-response, and artifact boundaries. SafePinned preparation now seals its exact public/private SSRF helper graph, resolver alias, normalization, peer-address, and TLS capabilities; access/prepared validation rejects drift before DNS or connection I/O.
- Round 11 ledger result: `add_observation` reloads the exact durable child inside the same storage turn, requires full dataclass equality, and derives identity/discovery provenance only from that durable row before insertion. Forged or stale task input fails with `observation.task_conflict` before any observation write.
- Round 11 verification: hardening is `119 passed`; focused Agentic/access/unified-read/site-diagnostic is `324 passed`. Local Ruff 0.15.12 default and preview, Ruff format, Python 3.14 compile, and `git diff --check` pass; final source retains Python 3.12 PEP 695 syntax. Python 3.12 is not installed locally, so the manager retains dependency-complete Python 3.12 and native Ruff 0.16.4/full-suite validation. Content scope remains exactly twelve paths and `immutable_artifacts.py` still has no content diff.
- Round 12 TDD evidence: the first exact new selection was `3 failed, 14 passed, 119 deselected`; after the bounded corrections and one explicit two-thread lock-order regression, the final Round 12 selection is `18 passed, 119 deselected`.
- Round 12 authority result: prepared authority now owns an exact immutable Agentic predicate dispatch and semantic seal spanning rule matching, canonicalization, URL-origin/splitting, glob matching, and compiled-prefix containment. Run execution captures one original prepared authority, repository, storage, artifact store, read gateway, and authority identity; all scheduling, read, persistence, interruption, and result paths use that local snapshot and revalidate the original private pointers before effects.
- Round 12 transport/reservation result: sealed SafePinned execution captures and validates DNS lookup, socket connection, TLS context/classification, HTTP/HTTPS connection constructors, peer IP policy, URL splitting/joining, quoting, dot-segment removal, domain validation, and host-header normalization before DNS/connect. Origin reservation now releases its internal lock before any external target claim, then revalidates claim progress, policy freshness, reservation window, and budget slot before recording the true request start; deterministic expiry and storage/origin lock-order tests prove zero stale send and no inversion deadlock.
- Round 12 verification: hardening is `136 passed`; focused Agentic/access/unified-read/site-diagnostic is `324 passed`; final Round 12 exact is `18 passed, 119 deselected`. Local Ruff 0.15.12 default/preview, Ruff format, Python 3.14 compile, and `git diff --check` pass after restoration of Python 3.12 PEP 695 syntax. Manager retains native Ruff 0.16.4, dependency-complete Python 3.12, and full-suite validation. Content scope remains exactly twelve paths.
- Round 12 audit follow-up: bound-rule seed canonicalization and allowed-origin parsing now use the prepared immutable predicate dispatch, materialize their results before evaluation, and revalidate the complete rules/prepared/helper seal immediately afterward and again before return. A deterministic post-entry helper mutation regression is GREEN together with the existing helper matrix (`6 passed, 132 deselected`), with zero ledger or I/O effects.
- Round 13 TDD evidence: the first exact batch reproduced `9 failed, 138 deselected` across frozen run-snapshot methods, five access-decision canonicalization helpers, SafePinned normalization dispatch, the per-run crawler snapshot pointer, and Mock preparation graph publication. The minimal implementation plus the ordered-start/failure-wake regression reached `10 passed, 138 deselected`; twelve supplemental socket/SSL/HTTP unbound-method drift cases bring the final Round 13 selection to `22 passed, 138 deselected`.
- Round 13 authority/concurrency result: run-snapshot behavior is exact-type and invoked through frozen unbound methods with before/after validation; prepared predicate sealing now includes the complete access-decision URL/query/sensitive-data canonicalization graph. Execution snapshots retain the exact crawler/search snapshots and all lazy adapter output uses those local identities. SafePinned sealing includes its normalization method/helpers and exact socket, SSL, HTTP connection/response method capabilities before DNS/connect. Mock preparation validates its full preview/build/commit graph under the one sealed state lock and restores the exact prior dictionaries/origins/robots mode on any publication failure. Per-origin actual-start tickets are assigned only after the external send-claim callback returns, serialize ready sends, recompute pacing/policy at their turn, and retire atomically on failure so later waiters wake without reintroducing the storage/origin lock inversion.
- Round 13 verification: exact Round 13 is `22 passed`; focused Agentic orchestration/hardening is `159 passed`; expanded Agentic/access/unified-read/site-diagnostic coverage is `472 passed`, and the final SafePinned/access regression rerun is `297 passed`. Before restoring the required Python 3.12 PEP 695 declarations, all tests ran under the local dependency-complete Python 3.11 environment with only the existing `asyncio_mode` and Starlette/httpx warnings. After restoration, local Ruff 0.15.12 default and the corresponding `I,UP,PIE,BLE,FURB,B023,ISC004,RET501,PLR1711` preview selectors pass, the seven formatter-owned files pass Ruff format, Python 3.14 compile passes, and `git diff --check` is clean. Content scope remains exactly twelve paths; `immutable_artifacts.py` still has no content diff. Manager retains native Ruff 0.16.4, dependency-complete Python 3.12, and full-suite validation.
- Round 13 publication-atomicity follow-up: a same-output `_url_origin` implementation drift reproduced the remaining post-commit hole (`1 failed`): Mock gateways/origins/robots mode were published before the first complete predicate-dispatch validation, then preparation leaked a late raw `ValueError`. Preparation now validates and seals the full predicate graph before preview, computes expected origins only through that captured dispatch, and revalidates every preconstructed input before publication. The single Mock commit and final `PreparedAgenticAuthority` construction execute under the exact state lock; any post-commit `BaseException` restores the original gateway dictionary object/content, prepared origins, and robots mode before the lock is released. Exact residual is `1 passed`; all Round 13 cases are `23 passed, 138 deselected`; focused Agentic coverage is `172 passed`; expanded Agentic/access/unified-read/site-diagnostic coverage is `485 passed`. No public behavior or scope expanded.
- Round 14 TDD evidence: one bounded batch added exactly five initially failing regressions for signed pacing-slot inversion, the SafePinned `NormalizedOrigin` constructor, frozen prepared-authority validation, frozen adapter invocation, and epoch-proven target-send progress. Exact RED was `5 failed, 161 deselected`; exact GREEN is `5 passed, 161 deselected`.
- Round 14 authority/concurrency result: actual target start now preserves the signed authorization `not_before` even when external callbacks complete out of order. SafePinned normalization captures and validates the exact `NormalizedOrigin` constructor/type before DNS or connection. Prepared authority and adapter validation/invocation use frozen unbound call routes with before/after identity checks, preventing scope enlargement or candidate publication through method replacement. Agentic target-send leases seal their class methods, repository renew/release dispatch, repository/storage/clock/fence identities, claim binding, timeout, lock, and exact gateway progress bridge; an expired or replaced renewal capability cannot produce a stale target send or disturb a successor epoch.
- Round 14 verification: hardening is `166 passed`; focused Agentic orchestration is `177 passed`; expanded Agentic/access/unified-read/site-diagnostic coverage is `490 passed`. Tests ran under the local dependency-complete Python 3.11 environment using the established temporary generic-syntax compatibility shim, then the required Python 3.12 PEP 695 declarations were restored. Local Ruff default and preview selectors, formatter-owned files plus touched Site Diagnostic ranges, Python 3.14 compile, and `git diff --check` pass. Content scope remains exactly twelve paths; manager retains native Ruff 0.16.4, dependency-complete Python 3.12, and full-suite validation.
- Round 14 authoritative-static follow-up: manager's two bounded Ruff findings were fixed mechanically without behavior changes: the sealed target-send `__call__` identity now uses the exact class dictionary instead of dynamic special-method lookup, and `_TargetSendLease.__slots__` is natural-sorted. Exact Review 14 remains `5 passed, 161 deselected`; hardening remains `166 passed`. Local Ruff default and preview including `B004,RUF023`, format, Python 3.14 compile with final PEP 695 syntax, and `git diff --check` pass.
- Round 15 final TDD evidence: exactly two accepted trust-root regressions first failed as intended (`2 failed, 166 deselected`): a self-restoring live `re.sub` rewrote `/allowed/item` after signed authorization, and a paired permissive Agentic predicate dispatch/validator rebind survived preparation. The exact selection is now `2 passed, 166 deselected`.
- Round 15 result/verification: SafePinned transport captures exact regex search/substitution abilities in its immutable dispatch, invokes only those captured callables, and seals the live identities before normalization/send, so drift fails with stable `transport_integrity` before target/DNS effects or artifact evidence. Agentic preparation, construction, and validation use only canonical frozen predicate-dispatch/validator roots and reject any live pair drift with `authority.seal_invalid` before Mock publication, ledger creation, or I/O. Hardening is `168 passed`; expanded Agentic/access/unified-read/site-diagnostic coverage is `492 passed`. Local Ruff 0.15.12 default and the established Ruff 0.16-equivalent preview selectors pass, as do Ruff format, Python 3.14 compile with final Python 3.12 PEP 695 syntax, and `git diff --check`. Content scope remains exactly twelve paths; manager retains native Ruff 0.16.4 and dependency-complete Python 3.12/full-suite validation.
- Round 15 predicate-root direct-audit follow-up: rebinding the live dispatch/validator and both `_FROZEN_*` aliases reproduced one remaining self-authorization path (`1 failed, 168 deselected`). The canonical pair is now bound once during module initialization in a private root cell captured by the prepare/create/validate wrappers; those paths compare all four public aliases to the closure-held pair before invoking the local canonical validator or dispatch, so four-way rebinding fails before Mock publication, ledger creation, or I/O. The isolated case is `1 passed, 168 deselected`, complete Round 15 is `3 passed, 166 deselected`, hardening is `169 passed`, and expanded relevant coverage is `493 passed`.
- Round 15 native-static follow-up: replaced the constant-name `getattr` assertion in the regex-drift regression with exact direct attribute access, clearing manager Ruff 0.16.4 `B009` without behavior change. Round 15 remains `3 passed, 166 deselected`; local Ruff default and explicit `B009`, format, Python 3.14 compile with final PEP 695 syntax, and `git diff --check` pass.
