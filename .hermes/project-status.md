# Project Status

- Date: 2026-05-12
- Project: web_listening
- Branch: codex/pr3-acquisition-cli-api
- Run type: PR3 scoped implementation worker plus review fixes.
- Scope: standalone acquisition tool catalog/profile/probe surface exposed through CLI and REST API; fixed staged workflow execution remains unchanged.
- Code changes: added `web_listening/blocks/acquisition_tools.py` with a stable `acquisition-tools.v1` catalog for `web_http`, `browser_rendered`, `sitemap`, `rss`, `cloakbrowser`, and `batch_python`; added default profile payload construction over PR1 profile builder; added one-URL PR2 capture probing with http/https URL validation, list-only adapter rejection, profile-path loading, default profile creation, available tool metadata, attempt payload, and next-action guidance. Follow-up fixes: `probe_acquisition_url` and `_load_or_build_profile` accept profile-path-only probing with `site_key` required only when no profile path is supplied; built-in probe adapters are now closed in a `finally` path after success or capture error, using adapter `close()` or `adapter.crawler.close()` when available.
- CLI/API changes: added `list-acquisition-tools`, `build-acquisition-profile`, and `probe-acquisition` commands; added `/api/v1/acquisition/tools`, `/api/v1/acquisition/profiles/default`, and `/api/v1/acquisition/probe`; API profile paths are restricted through the existing `settings.data_dir` path guard and API does not write profile output files. Follow-up fixes: CLI/API `probe` `site_key` is optional when `profile_path` is provided and returns the helper's clear error when neither is provided; CLI now maps probe-time `ValueError` into `typer.BadParameter` for malformed profile inputs.
- Tests: added CLI help/catalog/profile/probe tests and API tools/default-profile/probe tests, with network-free probe behavior via monkeypatching; added profile-path-only CLI/API probe tests that load a profile under test and fake the built-in adapter, plus missing-site-key-without-profile-path rejection and acquisition profile path guard coverage; added focused network-free cleanup regressions proving built-in adapters/crawlers close after successful probes and after capture exceptions. Confirmed red tests before implementation for the initial PR3 surface, the profile-path-only gap, and the adapter cleanup gap.
- Verification: `.venv\Scripts\python -m pytest tests\test_cli.py::test_list_acquisition_tools_json_lists_probe_capabilities tests\test_api.py::test_acquisition_tools_endpoint_returns_catalog -q` failed red as expected before initial implementation; `.venv\Scripts\python -m pytest tests\test_cli.py::test_probe_acquisition_allows_profile_path_without_site_key tests\test_cli.py::test_probe_acquisition_requires_site_key_when_no_profile_path tests\test_api.py::test_acquisition_probe_endpoint_loads_profile_path_without_site_key tests\test_api.py::test_acquisition_probe_endpoint_requires_site_key_without_profile_path -q` failed red as expected before the profile-path fix; `.venv\Scripts\python -m pytest tests\test_cli.py::test_probe_acquisition_closes_built_in_adapters_after_success tests\test_cli.py::test_probe_acquisition_closes_built_in_adapters_after_capture_exception -q` failed red as expected before the cleanup fix and passed after it; `.venv\Scripts\python -m pytest tests\test_cli.py tests\test_api.py tests\test_acquisition_capture.py tests\test_acquisition_profile.py -q` passed with 76 tests; `.venv\Scripts\python -m pytest tests -q` passed with 230 tests; `.venv\Scripts\python -m web_listening.cli --help` exited 0; `.venv\Scripts\python -m web_listening.cli list-acquisition-tools --json` exited 0 and emitted parseable JSON; `git diff --check` exited 0 with line-ending warnings only.
- Current conclusion: PR3 acquisition selection/probing is available as a standalone CLI/API contract layer while discover/classify/scope/bootstrap/run/report/manifest behavior is untouched.
- Next recommended action: review the scoped diff, then commit/push/PR from this branch when ready.
